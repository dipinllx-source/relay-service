## 1. 导入时补写索引（D1a / D2 / D4）

- [x] 1.1 `src/services/backupService.js` 的 `restoreEntityItems(items, bucket)` 签名扩展为 `restoreEntityItems(items, bucket, opts)`，`opts = { prefix, indexWriter, indexLabel, indexStats }`；不传 `opts` 时行为与现状完全一致（tags / admins 通道不受影响）。索引写法 MUST 由调用方以 `indexWriter` 回调给出，MUST NOT 在函数内硬编码索引 key —— 账户侧是单一集合 `<前缀>index`，API Key 侧是 `apikey:idx:*` / `apikey:set:*` 索引族，二者不同构（见 1.6）
- [x] 1.2 实体写回**成功之后**（`bucket.imported++` 同一分支内）执行索引登记：`id = key.slice(prefix.length)`，调 `indexWriter(id, item)`；id MUST 从 Redis key 提取，MUST NOT 读实体内的 `id` 字段（D1）
- [x] 1.3 索引登记成功则 `indexStats[indexLabel]++`；抛错则 `bucket.errors++` + `indexErrorCount++` + `logger.error`，MUST NOT 降级为 warn（D2）
- [x] 1.4 被 `skipped` 的条目 MUST NOT 触发索引补写（保留「实体在、索引不在」这一不一致的可观测性，交给第 4 组的重建收敛）
- [x] 1.5 账户循环处传入各自的 `prefix`，`indexWriter` 为 `(id) => redis.addToIndex(\`${group.prefix}index\`, id)`（内含 `sadd` + `del <indexKey>:empty`）；索引 key MUST 由 `ACCOUNT_GROUPS` 已有的 `prefix` 派生，MUST NOT 另行硬编码
- [x] 1.6 apikey 通道的索引登记 MUST 走 API Key 侧真实在维护的索引族（`apikey:idx:*` / `apikey:set:*`），即调 `apiKeyIndexService.addToIndex({ id, name, createdAt, lastUsedAt, isActive, isDeleted, tags })`；备份里 `tags` 是 JSON 字符串须先解析成数组。该入口按 `isDeleted` / `isActive` 自动分入 `apikey:set:deleted` 或 `apikey:set:active`，故 MUST NOT 在导入侧另做「软删则不写索引」的判断。**MUST NOT 写 `apikey:index`** —— 该集合在代码中不存在（无写入方、无读取方），形状来自账户侧，写它等于造一个无人读取、必然漂移的派生数据（见 3.8 的核实结论）
- [x] 1.7 收尾的 `:empty` 标记清理保持原样（`addToIndex` 已逐个清过，此处为兜底，重复删除无害）
- [x] 1.8 收尾新增 `del account:index:version`，无条件执行（第 4 组的兜底触发器，D4）；与 apikey 侧**不对称**是有意的 —— 账户侧无索引快慢路径切换，删版本号只让下次启动多跑一次重建，代价近乎为零（对照 1.9）
- [x] 1.9 `del apikey:index:version` 改为**条件执行**：仅当本轮存在索引登记失败（`indexErrorCount > 0`）时才删。逐条登记已让索引族完整，无条件删会使 `isIndexReady()` 为假、列表查询在下次重启前一直退化为全量 SCAN；有失败时删掉则由下次启动的 `rebuildIndexes()` 兜底，期间回退 SCAN 结果仍正确。删除时 MUST 打 warn 说明原因

## 2. `apikey:hash_map` 字段级合并与不变式（D2 / E1 / D7）

- [x] 2.1 导入侧把 `__key === 'apikey:hash_map'` 的条目从 `data.apiKeys` 数组中摘出，交由新函数 `restoreHashMap(item, stats)` 处理，剩余条目走实体通道；该处理 MUST 排在 `restoreEntityItems` 写完 API Key 实体**之后**
      —— **任务原文已按实测修正（原写「之前」）**：9.3 用例第一次跑就暴露了顺序缺陷 —— 备份里映射与实体成对出现，先合并映射时 2.5 的 `exists(apikey:<keyId>)` 复核对每一条都为假，全部映射被判成「实体不存在」跳过（导入报告里只剩一串 `hashmap-entity-missing` 告警），还原的 key 依旧 401 到下次重启，本次要修的 D2 症状原封不动。已把 `for (const item of hashMapItems) await restoreHashMap(item)` 从实体写回前移到其后，并在 `restoreHashMap` 头部与调用点各写一段「顺序错了不报错、只静默退化」的注释。同步改了 design D7（原文明确写着「之前」，属设计层面的错，不是实现走偏）与 spec：新增顺序不变式，且把「导入顺序 SHALL 为分组、账户、API Key」补全为「分组、账户、API Key 实体、哈希映射」
- [x] 2.2 `restoreHashMap` 逐字段处理，跳过 `__key` / `__type` 两个元字段
- [x] 2.3 每个字段 `<hash> → <keyId>`：`hexists apikey:hash_map <hash>` 为真 → `skipped++` 并 `continue`，MUST NOT 覆盖（防止把同一 hash 改指到另一 keyId，D7）
- [x] 2.4 `hget apikey:<keyId> isDeleted` 为 `'true'` → `skippedDeleted++`，写入 `warnings`，MUST NOT 写映射（这是与第 3 组不变式的对接点，D7）
- [x] 2.5 目标实体 `apikey:<keyId>` 完全不存在 → `skipped++` 并计 warning（备份中有映射但实体未被还原，通常因实体被 `skipped` 或不在备份范围）
- [x] 2.6 以上均通过 → `hset apikey:hash_map <hash> <keyId>`、`imported++`
- [x] 2.7 `summarize()` 中 `apiKeys` 计数的 `filter((x) => x.__key !== 'apikey:hash_map')` 逻辑保持不变（摘出后仍需正确计数，确认不受 2.1 改动影响）
- [x] 2.8 导出侧 MUST NOT 改动 —— `apikey:hash_map` 继续收在 `data.apiKeys` 中，备份结构不因此变化（D7）

## 3. API Key 删除持久性不变式（E1 / D9）

- [x] 3.1 `src/services/apiKeyIndexService.js` 的 `rebuildHashMap()` 回填条件从 `if (keyData && keyData.apiKey)` 改为同时要求 `keyData.isDeleted !== 'true'`
- [x] 3.2 同一轮扫描内新增反向清理：`hgetall apikey:hash_map`，对每个字段的目标 keyId 判定 —— 实体不存在，或实体 `isDeleted === 'true'` → 该字段进待删列表
- [x] 3.3 「实体不存在」这一判定 MUST 二次 `exists` 复查后才删（规避瞬时读失败导致误删活跃 key 的映射，D9）
- [x] 3.4 清理与回填 MUST 基于同一次扫描结果、在同一轮内完成，MUST NOT 跨轮复用
- [x] 3.5 清理条数与 keyId 列表 MUST 记入 `logger.info`（该动作会让线上 key 失效，必须可追溯，D9）
- [x] 3.6 确认 `apiKeyService.deleteApiKey()` 的软删除路径无需改动 —— 它已正确移除映射，问题只在回填侧（已核实：`apiKeyService.js:1322` 调 `redis.deleteApiKeyHash`，并 `updateIndex` 把 id 移入 `apikey:set:deleted`）
- [x] 3.7 确认 `apiKeyService.restoreApiKey()`（`apiKeyService.js:1371` 附近）在恢复时会重新写入映射；若不写，恢复后的 key 将无法鉴权直到下次重启 —— 这是本次不变式收紧后新暴露的路径，MUST 核实并在缺失时补上（已核实：`apiKeyService.js:1396` 调 `redis.setApiKeyHash`，映射立即恢复，无需补代码）
- [x] 3.8 **核实结论：`apikey:index` 这个集合在系统中不存在**。`grep -rn "apikey:index" src/ web/ scripts/` 只命中三类派生 key —— `apikey:index:empty`（空标记）、`apikey:index:version`（版本标记）、`redis.js:558` 的 scan 排除前缀；集合本体无任何写入方与读取方，dev 43.110.32.63 实测 `exists apikey:index` = 0、`type` = `none`。proposal D4 由账户侧 `<前缀>index` 类推而来，把索引形状错配给了 API Key 侧。API Key 侧真实在维护的是 `apikey:idx:createdAt`（zset 26）/ `apikey:idx:all`（set 26）/ `apikey:set:active`（12）/ `apikey:set:deleted`（14）。据此：不变式改挂在该索引族上，导入侧改走 `addToIndex`（见 1.6），spec 两处与 proposal / design 相应措辞同步修正
- [x] 3.9 确认永久删除路径已满足索引族清理要求，无需改代码 —— `apiKeyService.permanentDeleteApiKey()`（`apiKeyService.js:1468`）调 `apiKeyIndexService.removeFromIndex()`，后者一次性 `zrem` 三个 zset、`srem` `idx:all` / `set:active` / `set:deleted` 与全部 `apikey:tag:*`；`redis.deleteApiKey()`（`redis.js:512`）再删 `apikey:hash_map` 字段与实体本身
- [x] 3.10 `apiKeyIndexService.addToIndex()` 内部把异常吞成 `logger.error` 而**不抛**，直接 await 无从判断成败。导入侧 MUST 在登记后显式复核 `sismember(INDEX_KEYS.ALL_SET, id)`，不成立则抛错交由 1.3 计入 errors —— 否则索引写失败会被计成成功，正是本次要消灭的「实体在、索引不在」。索引 key MUST 引用 `apiKeyIndexService.INDEX_KEYS`，MUST NOT 在导入侧硬编码字符串

## 4. 账户索引版本化重建（D1b / D3 / D4）

- [x] 4.1 新增 `src/services/accountIndexService.js`，导出 `checkAndRebuild()` 与 `rebuildAll()`；常量 `INDEX_VERSION_KEY = 'account:index:version'`、`CURRENT_VERSION = 1`
- [x] 4.2 账户前缀表 MUST 复用单一来源，MUST NOT 第四次抄写 —— 现有三处（`backupService.ACCOUNT_GROUPS` 对象数组、`metadataSync.ACCOUNT_GROUPS` 元组数组、各账户服务的 `*_KEY_PREFIX` 常量）已经漂移，本任务 MUST 从其中一处 require 复用并在注释中标明来源（已落地：`src/storage/metadataSync.js` 新增导出 `ACCOUNT_GROUPS` 与 `isEntityKey`，本服务从该处 require；选它而非 backupService 的理由是它本职就是「枚举 Redis 中全部账户实体」，且层级低于 services、无反向依赖）
- [x] 4.3 `rebuildAll()` 逐前缀：`scanKeys(<前缀>*)` → `isEntityKey` 过滤 → `liveIds`；`smembers <前缀>index` → `indexedIds`（用 `redis.scanKeys` 的 SCAN 实现，MUST NOT 用 `client.keys` 阻塞主线程；已核实 11 个前缀的索引 key 均为 `<前缀>index`，代码中 11 个字面量齐备）
- [x] 4.4 `liveIds - indexedIds` → `sadd`，计数上报（按 500 分批，避免单条命令参数过长）
- [x] 4.5 `indexedIds - liveIds` → `srem`，计数上报（孤儿清理，D3；同样分批）
- [x] 4.6 `liveIds` 非空 → `del <前缀>index:empty`；`liveIds` 为空则 MUST NOT 主动打 `:empty` 标记（那是 `getAllIdsByIndex` 的职责，重复打会缩短其 TTL 语义）
- [x] 4.7 单前缀失败被隔离（warn + errors 计数），MUST NOT 中断其余前缀
- [x] 4.8 全部前缀成功后写 `INDEX_VERSION_KEY = CURRENT_VERSION`；有任一前缀失败则 MUST NOT 写版本号（下次启动重试），并打 warn 说明未写入
- [x] 4.9 `checkAndRebuild()` 读版本号，`>= CURRENT_VERSION` 直接返回；否则**后台异步**调 `rebuildAll()`，MUST NOT 阻塞启动（与 `apiKeyIndexService` 一致；另有 `isBuilding` 并发守卫）
- [x] 4.10 `src/app.js` 在 `apiKeyIndexService.checkAndRebuild()` 之后调用 `accountIndexService.checkAndRebuild()`（`await` 的只是版本号读取，重建本身仍在后台）
- [x] 4.11 确认与 `metadataSync` 定时轮无互斥问题：本服务只读 Redis 实体、只写 Redis 索引集合，不碰 SQLite；`metadataSync` 只读 Redis、只写 SQLite，两者无共享写目标。另核实 require `metadataSync` **不会**启动其定时器（`start()` 需显式调用，模块体无副作用），且 `redis.js` 未 require `metadataSync`，不构成循环依赖 —— dev 上 `node -e require(...)` 冒烟通过

## 5. 分组子系统纳入备份（D3 / 决策 D5 / D6 / D10）

- [x] 5.1 `BACKUP_VERSION` 改为 `'2.2'`（并注明旧代码读 2.2 备份时只是忽略 `data.groups`，等价其当前行为，故回滚安全）
- [x] 5.2 导出新增 `data.groups`：`definitions`（`account_group:<gid>` 逐个 `hgetall`，形如 `{ __key, ...fields }`）、`index`（`smembers account_groups`）、`members`（`{ '<gid>': [...] }`）、`reverse`（`{ 'account_groups_reverse:<platform>:<id>': [...] }`）。四类 key 全部取自 `accountGroupService` 导出的 `GROUPS_KEY` / `GROUP_PREFIX` / `GROUP_MEMBERS_PREFIX` / `REVERSE_INDEX_PREFIX` 常量，MUST NOT 再抄一份字面量
- [x] 5.3 `account_groups_reverse:*` 扫描 MUST 显式排除 `account_groups_reverse:migrated`（key 形状只有一段，`isEntityKey` 的冒号规则不适用，D5）—— 扫描走 `redis.scanKeys()` 后 `.filter((k) => k !== accountGroupService.REVERSE_INDEX_MIGRATED_KEY)`
- [x] 5.4 `account_groups_reverse:migrated` MUST NOT 纳入备份（否则目标实例会永久跳过反向索引回填，D5）—— dev 真机导出实测 5 个 reverse key 中无 `:migrated`
- [x] 5.5 单个分组读取失败隔离（warn + `errors.groups++`），MUST NOT 使整个导出失败（per-gid 与 per-reverse-key 各自 try/catch）
- [x] 5.6 导入顺序 MUST 为 **分组 → 账户 → API Key**（D8）—— 分组段整块前置，账户块随之上移，原先位于 tags 之后的重复账户块已删除
- [x] 5.7 `definitions` 按实体语义跳过冲突（`exists` → skipped，D6）—— 复用 `restoreEntityItems(definitions, stats.groups.definitions)`，不传 `indexWriter`（分组索引由 5.8 的成员合并单独处理）
- [x] 5.8 `index` / `members` / `reverse` 按 `sadd` 成员合并；`members` 新增成员数计入 `stats.groups.members.added`，`reverse` 同理（D6）
- [x] 5.9 分组定义被 skipped 但其成员集合有新增时，MUST 写一条 `warnings`（现存分组被塞入旧备份的成员，D6）—— 导入前先用 `exists` 采集 `preExistingGids`，再据此判定，避免被 `restoreEntityItems` 写回后误判
- [x] 5.10 `summarize()` 增加 `groups` 计数（取 `definitions.length`），供导出摘要与 UI 展示 —— dev 实测 `{"apiKeys":25,"accounts":5,"tags":0,"admins":1,"groups":2}`

## 6. 悬空 groupId 处置（D3b / D3c / D8）

- [x] 6.1 导入时判定备份是否含 `data.groups` 段；**不含**（`2.0` / `2.1`）时对写回的账户实体剥离 `groupId` 与 `groupIds` 两个字段
      —— `restoreEntityItems` 新增 `sanitize` 钩子，账户通道传 `sanitize: hasGroups ? undefined : stripDanglingGroupRefs`。`stripDanglingGroupRefs` 返回**新对象**而非原地改：备份对象在导入后仍会被 `summarize` 读（smoke6 A10 已验证二次读数仍为 2）。bedrock 的引用藏在 JSON 字符串里，故 `__type === 'string'` 分支先 parse 再剥再序列化，parse 失败只 warn 不阻断（A4/A5）
- [x] 6.2 判据 MUST 是「备份是否含 groups 段」，MUST NOT 是「分组当前是否存在」（后者受导入顺序影响会误判，D8）
      —— 判据取自 `hasGroups`（即 `data.groups` 是否存在），与「分组是否存在」无关；第 5 组已把分组段整块前置到账户之前，若按存在性判定，刚被还原的分组会让剥离逻辑反过来失灵
- [x] 6.3 每次剥离 MUST 写一条 `warnings`：账户名、原 `groupId`、已解除绑定
      —— `addWarning('dangling-group-stripped', ...)`，上下文含 `accountKey` / `accountName` / 原 `groupId` / 原 `groupIds`（smoke6 A6–A8）。剥离排在 `exists` 判定**之后**：被跳过的实体压根没写进去，不该为它报「已解除绑定」（C1/C2 验证二次导入 2 个全跳过且 0 条告警）
- [x] 6.4 含 `data.groups` 的 `2.2` 备份 MUST NOT 剥离
      —— smoke6 B1/B2：`2.2` 备份里指向不存在分组的 `groupId` 原样落库、无告警，分组随备份一并还原故无需剥离
- [x] 6.5 账户更新路径新增悬空校验：保存前若 `groupId` 指向的 `account_group:<gid>` 不存在，则从待写数据中移除该字段 + `logger.warn`，然后继续保存，MUST NOT 返回 500（D3c）
      —— 新增共享助手 `src/routes/admin/utils.js` 的 `pruneDanglingGroupRefs(updates, platform, accountId)`；选这里落地是因为它已被全部账户更新处理器 import（`mapExpiryField` 的同一个收口），无需在 8 个路由里各写一遍。8 个带分组绑定的更新处理器逐个接线：ccr / claude / claudeConsole（`'claude'`）、gemini / geminiApi（`'gemini'`，后者原先没引 `./utils`，本次补了 require）、openai / openaiResponses（`'openai'`）、droid（`'droid'`）。droid 的绑定读的是 `delete` 之前摘出来的局部变量，故包一层 `groupRefs = { groupId, groupIds: normalizedGroupIds }` 并把绑定分支的 4 处引用改读 `groupRefs`。azureOpenai 的更新处理器只在**创建**路径绑分组，bedrock / openaiCompatible 没有分组绑定路由，故不在范围内（已用 `addAccountToGroup|setAccountGroups` 全量 grep 复核 routes 目录）。smoke6 D1–D4/D6–D8：悬空 gid 被摘掉、真实 gid 保留、其他字段不动、无引用/空入参早退不抛错
- [x] 6.6 `accountGroupService.updateGroup()` 抛「分组不存在」的行为 MUST NOT 改动 —— 更新不存在的分组本就该报错，缺陷在账户保存流程把它冒泡成 500（D8）
      —— `accountGroupService.js` 本次零改动；唯一的 `updateGroup()` 调用方 `accountGroups.js:68` 也未改。修的是账户侧「绑定前先剔除悬空引用」，`addAccountToGroup` / `setAccountGroups` 的抛错语义保持原样
- [x] 6.7 确认自动解绑同时清理 `account_groups_reverse:<platform>:<accountId>` 中的残留 gid
      —— 助手在剔字段之后对 `account_groups_reverse:<platform>:<accountId>` 做 `srem`（失败只 warn，不阻断保存）；不清的话账户列表仍会显示这个幽灵分组。smoke6 D5：预置 `{ghost1, ghost2, 真实gid}` 的反向键，执行后只剩真实 gid `5debcbec-…`

## 7. 启动期密钥一致性自检与迁移文档（缺陷 D5 / 决策 D12 / D13）

> 本组是**缺陷 D5**（备份密文与 `ENCRYPTION_KEY` 硬绑定，换机即静默失效）的全部落地内容 —— 一个只读的启动自检加一段文档。跨密钥重加密、导入前置闸门、`API_KEY_PEPPER` 三项已否决，分析结论见 design「已否决方案」。注意与第 5 组标题里的「决策 D5」（`account_groups_reverse:migrated` 不纳入备份）不是同一件事。

- [x] 7.1 新增 `src/services/encryptionKeyCheckService.js`，导出单个入口（如 `check()`）；本服务**只读** Redis，MUST NOT 写入任何 key、MUST NOT 改动任何实体字段
      —— 新文件 17KB，对外只导出 `check()`（另有 `_internal` 仅供测试）。全程只用 `smembers` / `hgetall` / `get` 三个读命令，无一处写。dev 实测：执行前后对全库 key 集合与 `dbsize` 做 diff，零差异（含没有生成任何 `<前缀>index:empty` 标记）；解密只在内存里做，取到的实体对象不回写
- [x] 7.2 抽样取账户：逐前缀经 `redisClient.getAllIdsByIndex()` 取 id 后设上限（每平台 ≤3 个账户），MUST NOT 全库 SCAN、MUST NOT 全量解密，以免拖慢启动
      —— **实现偏差（已记入服务头部注释）**：没有用 `getAllIdsByIndex()`，改为只读的 `smembers('<平台前缀>index')`。原因是该助手（`redis.js:5042`）在索引为空时会 `setex <indexKey>:empty 3600`、走 SCAN 兜底时还会 `sadd` 回填索引 —— 两个都是写操作，与 7.1 的「MUST NOT 写入任何 key」直接冲突，二者只能保一个，保了只读。放弃兜底是安全的：自检排在 `accountIndexService.checkAndRebuild()` 之后（7.12），索引此刻已收敛，`smembers` 拿到的就是全量；真取不到就退化为 7.8 的「无法判定」，而这正是新装机器的正常结论。上限 `SAMPLE_LIMIT = 3`，仅在 `fail === 1 && ok === 0` 时升到 `EXTENDED_LIMIT = 8`（7.6），绝无全库 SCAN 与全量解密
- [x] 7.3 探针优先取聚合型 JSON 字段（`claudeAiOauth` / `openaiOauth` / `geminiOauth`），无则退到单值字段（`accessToken` / `apiKey` 等）
      —— `PLATFORMS` 探针表覆盖 11 个平台，每项 `{ platform, indexKey, keyOf, storage, decrypt, fields }`，`fields` 数组按顺序排，JSON 型（`kind: 'json'`）一律排在单值型前面，命中即停。`decrypt` 用惰性 `require` thunk 直接调各平台**自己**的解密入口，避免在自检里重写一遍 11 套密钥派生；这些服务单例在 `app.js` 早已加载，不会因此多起定时器。bedrock 的实体存的是 JSON 字符串（`storage: 'string'`），走 `get` + `JSON.parse` 而非 `hgetall`
- [x] 7.4 判据 MUST 落在**返回值**上，MUST NOT 写成 `try { decrypt(x) } catch { 判失配 }` —— `commonHelper.js:69` 失败时 `return text`、`claudeAccountService.js:2183` 失败时 `return encryptedData`，两条路径都不抛错，catch 永远进不去（D12，这是本组唯一真正容易写错的地方）
      —— 判定全部走 `judgePlain(cipher, plain, kind)`，看返回值：`plain === cipher` 即判 fail（这恰好就是那两条「不抛错」路径的失败表征）。唯一有 `try/catch` 的是 bedrock（`_decryptAwsCredentials` 是 11 个里唯一真会抛的），单独标 `kind: 'bedrock'` 并注明原因。smoke7 `unit` 模式 18 条断言里专门验了「传错密钥密文时 decrypt 返回原文且不抛」，确认 catch 写法在这里等于死代码
- [x] 7.5 判定规则：JSON 探针以 `JSON.parse` 成功为通过；单值探针以「返回值 !== 传入的密文原文」且「全为可打印字符」为通过
      —— `judgePlain`：空值或 `plain === cipher` → fail；`kind === 'json'` 要求 `JSON.parse` 成功**且**结果为 object（parse 出裸数字/字符串也算 fail，防乱码侥幸过关）；单值型用 `PRINTABLE_RE = /^[\x20-\x7e\t\r\n]+$/`。JSON 优先是有必要的：AES-256-CBC 无 MAC，错密钥仍有约 1/256 概率通过 padding 校验产出乱码明文，JSON 探针能把这条漏网路径基本堵死
- [x] 7.6 单个字段失败 MUST NOT 直接定论：对该平台追加抽样后再判（宁可漏报不可误报，误报几次之后这条日志就没人看了，D12）
      —— 平台级判定三档：有任一 ok → `consistent`；`fail >= 2` → `inconsistent`；`fail === 1 && ok === 0` → `suspect`（先把预算从 3 升到 8 追加抽样，样本仍不足则只作旁证、不单独定论）。dev 用故意写错的密钥实测：openai 攒到 4 条失败 → `inconsistent`；claude 全库只有 1 条密文，追加抽样也补不出第二条 → 停在 `suspect`，最终日志里只作为「指向同一结论」的旁证出现，没有被当成独立证据
- [x] 7.7 legacy scheme C 形态（裸 hex、无 `iv:` 前缀）MUST 排除在一致性判定之外，另行以 warn 单独报告 —— 该形态在 Node 24 上无论密钥对不对都解不开，不构成密钥失配的证据
      —— `classifyCipher` 用 `CIPHER_RE = /^[0-9a-f]{32}:[0-9a-f]+$/i` 认 scheme A/B，`LEGACY_RE = /^[0-9a-f]{32,}$/i` 认 scheme C；legacy 命中的字段既不计 ok 也不计 fail，只进 `legacy` 桶，收尾用一条独立 `logger.warn` 报告。dev 无 legacy 数据，故用 stub 掉 `redis.getClientSafe` 的方式造出该形态验证（smoke7 `stub` 模式），确认结论为「无法判定」而非「不一致」，且 legacy warn 单独成行
- [x] 7.8 库中无任何非空密文字段时 MUST 判为「无法判定」并以 info 记录跳过原因，MUST NOT 报为不一致（这是新装机器的正常状态）
      —— 全局判定优先级 `inconsistent` > `consistent` > `inconclusive`，一条密文都没抽到时落 `inconclusive` 并以 info 记「库中暂无可抽样的密文字段（新装实例的正常状态）」。smoke7 `stub` 模式验证了「索引为空」与「实体存在但密文字段全空」两种入口都走这条，且不产出任何 error/warn
- [x] 7.9 判定不一致 → `logger.error`，列出受影响平台与各平台抽样失败数；判定一致 → `logger.info` 记录通过与抽样条数，MUST NOT 产生 warn 级噪音
      —— error 行形如 `🔑 ENCRYPTION_KEY 与库中已有密文不匹配：openai(失败 4 条: openaiOauth,…)。当前密钥指纹 28526c601022。… 另有 claude 各仅 1 条密文且同样未解出，样本不足以单独定论但指向同一结论。`（`alsoSuspect` / `alsoGood` 两段按需拼接）；一致时只有一行 info：`🔑 ENCRYPTION_KEY self-check passed (抽样 2 条，平台 claude、openai，200ms，指纹 87389747817f)`，dev 实测该路径零 warn 零 error
- [x] 7.10 日志 MUST NOT 输出 `ENCRYPTION_KEY` 明文、密文原值、解密所得明文（含片段）；只输出平台、账户 id / name、字段路径与结论
      —— 日志拼接只取平台名、账户 id/name、字段名、计数与结论；密文与明文变量从不进模板。smoke7 在 `real` 与 `wrong` 两种模式下都截获全部日志行做断言：不含 `ENCRYPTION_KEY` 取值、不含被抽样字段的密文子串、不含解出明文里的 token 片段
- [x] 7.11 若日志需标识当前密钥，指纹 MUST 为 `crypto.scryptSync(KEY, '<固定 salt>', 16)` 截断；MUST NOT 用 `sha256(KEY)` —— 该摘要正是 `bedrockAccountService.js:668` 的 `_encryptionKeyCache`，即 bedrock 实际使用的 AES-256 密钥（D12）
      —— `keyFingerprint()` = `scryptSync(config.security.encryptionKey, 'relay-key-check-fingerprint', 16).toString('hex').slice(0, 12)`。salt 与任何平台的派生 salt 都不同名，全文件无 `sha256` 调用（已 grep 复核）；smoke7 `unit` 断言指纹 !== `sha256(KEY)` 的任意前缀，即日志里那 12 个 hex 不能被拼回 bedrock 的可用密钥
- [x] 7.12 `src/app.js` 在 `accountIndexService.checkAndRebuild()` 之后**异步**调用；自检自身抛出的异常 MUST 被捕获，MUST NOT 阻断启动或影响 `/health`
      —— 插在 `await accountIndexService.checkAndRebuild()` 之后：自检靠 `<前缀>index` 取样，索引先收敛才不会误判成「库中无密文可抽」（这个顺序是硬依赖，注释里写明了）。不 `await`，`.catch()` 里降级为 `logger.warn('🔑 Encryption key self-check failed to run: …')`。**运行时行为已在第 9 组实测**：`systemctl restart relay-app` 后 `logs/claude-relay-2026-08-19.log` 出现 `🔑 ENCRYPTION_KEY self-check passed (抽样 2 条，平台 claude、openai，94ms，指纹 87389747817f)`，与它同一秒还有 `accountIndexService` 的重建日志（证实排在其后），`/health` 同时返回 200，服务 `active (running)`
- [x] 7.13 本组 MUST NOT 改动任何加解密实现 —— `commonHelper.createEncryptor`、各账户服务的私有加解密、`apiKeyService._hashApiKey`、`config/config.js`、备份路由签名全部零改动，自检只以既有 `decrypt` 做只读调用
      —— `git status` 复核：本组只新增 `src/services/encryptionKeyCheckService.js` 一个文件，加改 `src/app.js` 的 15 行调用；`commonHelper.js` / 各 `*AccountService.js` / `apiKeyService.js` / `config/config.js` / `routes/admin/backup*` 全部未出现在 diff 里。自检对 11 套解密一律只做只读调用，未复制、未包装、未改签名
- [x] 7.14 `.env.example` 与升级说明写明：`ENCRYPTION_KEY` 一旦库中已有数据即**不可更改**；跨机迁移 MUST 在目标机建立任何数据之前把该值设为源机的值，之后再导入备份
      —— `.env.example` 在 `ENCRYPTION_KEY=` 上方加 9 行注释（不可更改、错配表现为「管理台可见但上游全 401」、唯一支持的迁移顺序、启动日志自检那一行）；README 备份章节新增 `#### 跨机迁移与 ENCRYPTION_KEY`，插在「两条路径的取舍…详见 docs/metadata-storage-guide」与 `### 升级指南` 之间，含一段可照抄的 bash（取源机值 → 改目标机 `.env` → 重启 → **然后**才导入）与三行自检日志样例
- [x] 7.15 文档 MUST 说明本次不提供跨密钥重加密与导入前置闸门，并指向 design「已否决方案」中的两个例外场景（旧密钥泄露必须轮换、两台各自有数据的实例合并）
      —— README 该节写明「不提供跨密钥重加密，也不在导入时做密钥闸门（错配时闸门本身也无从判断该拦谁）」，并点出确实需要重加密的两个场景（旧密钥泄露必须轮换、两台各自有数据的实例合并），给出的做法是回源机重录凭据而不是搬密文。同节另附备份文件属机密、勿入代码仓库或公开桶的警示（对应第 8 组导出面板的同一句提示）
- [x] 7.16 文档 MUST 提示 `scripts/manage.sh:461` 会为新装机器生成随机 `ENCRYPTION_KEY`，迁移场景须在首次启动前覆盖该值 —— 这是最容易踩反的一步（先起服务建了数据，再改密钥就晚了）
      —— README 与 `.env.example` 两处都写了：一键安装脚本会为新装机器生成随机密钥，「先起了服务、建了管理员和账户，再回头改密钥就晚了 —— 那批新数据会连同旧备份一起解不开」。两个文件已推到 dev（`git diff --stat` 显示 `.env.example` +9 / `README.md` +35），仅文档改动，无需 lint

## 8. 导入结果结构与前端（D11）

- [x] 8.1 `stats` 新增 `groups` / `indexes` / `hashMap` / `warnings` 四段，现有四桶结构不变
      —— `backupService.js:271` 的 `stats` 初始化：原四桶（`apiKeys` / `accounts` / `tags` / `admins`）字段一字未改，新增段落集中列在下面并带注释「以下为本次新增（D11）」。`groups` 拆 `definitions` / `members.added` / `reverse.added` 三小节，`indexes` 是 `{ '<索引key>': 补写条数 }` 的动态字典（由 `ACCOUNT_GROUPS` 的 prefix 派生，不硬编码），`hashMap` 含 `imported/skipped/skippedDeleted/errors`
- [x] 8.2 `warnings` 每条形如 `{ type, message, ...上下文 }`；`type` 取值覆盖 `dangling-group-stripped`、`hashmap-skipped-deleted`、`group-members-merged-into-existing`、`hashmap-entity-missing`
      —— 统一收口在 `addWarning(type, message, context)`（`backupService.js:288`），各处不自行拼结构。四个 type 全部落地并各带上下文：`hashmap-entity-missing`（`{keyId}`）、`hashmap-skipped-deleted`（`{keyId}`）、`group-members-merged-into-existing`（`{groupId, added}`）、`dangling-group-stripped`（`{accountKey, accountName, 原 groupId/groupIds}`，hash 与 JSON 字符串两条分支各一处）
- [x] 8.3 `web/admin-spa` 导入结果视图展示新增四段；`warnings` 逐条列出，非空时视觉上须能被注意到（不能折叠进「成功」提示里）
      —— `StorageHealthSection.vue` 改为把整个响应存进 `importResult`，视图分三块：绿色汇总条（四桶）、灰色明细块（分组 / 索引补写 / 鉴权映射，每段缺失就不出现）、**独立的琥珀色告警块**（`border-amber-300` + 三角图标 + `<ul>` 逐条列出 message 并在后面缀 `[type]`）。告警块与成功条是两个兄弟元素，`v-if="importWarnings.length"`，不会被折进成功提示里；smoke8 A1–A8 逐项断言了汇总/三行明细/告警条数与「告警文案不出现在汇总里」
- [x] 8.4 导出结果 MUST 明确警示：备份文件含可解密的账户凭据与明文管理员凭据，须按机密文件保管；并写明「恢复到另一台服务器要求该机沿用同一 `ENCRYPTION_KEY`」
      —— 原来那句「仅可在相同 ENCRYPTION_KEY 环境下恢复」藏在灰色小字里，本次提成独立琥珀色警示框：标题「备份文件请按机密文件保管」，正文点明含可解密账户凭据与**明文管理员凭据**、不要进代码仓库或公开对象存储桶，第二段写明恢复到另一台机器要求沿用同一 `ENCRYPTION_KEY` 且**必须在该机建立任何数据之前**设好，并给出错配时的表现（导入「成功」、账户可见、上游全 401）—— 与第 7 组 README 的措辞一致。smoke8 D2/D3
- [x] 8.5 导出面板 MUST NOT 新增任何密钥输入控件（重加密已否决，前端不留半成品入口）
      —— 本次只加了展示性元素，没有任何 `input` / `v-model`；`http_apis.js` 的三个备份接口签名未动。smoke8 D1 用正则扫全文件，确认无 `type="password"`、无绑定到 key 的 `v-model`
- [x] 8.6 旧结构（缺新字段）在前端与后端读取路径均不报错
      —— 前端：三个 computed 全程 `?.` + `??`，`bucketText` 遇 `undefined` 返回 `null` 后被 `filter(Boolean)` 滤掉。smoke8 B1–B4 用只有 `apiKeys/accounts/admins` 的老结构验证「汇总照常、标签不占位、明细为空、warnings 为空」，C1–C6 再覆盖 `{}`、`indexes: {}`、`warnings` 非数组、`groups` 只有 `definitions`、`importResult` 为 null 五种边界，全部不抛。后端：唯一读 `stats` 的地方是 `routes/admin/backup.js:70` 的日志行，只取四桶（`tags` 已用 `?.`），新增字段对它透明
- [x] 8.7 `cd web/admin-spa && npx eslint --fix && npx vite build`
      —— 先只读跑一遍全量 `eslint`（不带 `--fix`）确认 SPA 原本零违规，再按任务原文跑带 `--fix` 的全量命令，`LINT_RC=0` 且 `git diff --stat web/` 仍只有 `StorageHealthSection.vue` 一个文件（+128/-9），没有顺手改到别处；`npx vite build` 成功（`✓ built in 15.01s`，`BUILD_RC=0`，只有既有的 chunk 体积告警）。产物已含新文案（`dist/assets/SettingsView-*.js` 里能 grep 到「按机密文件保管」「需要留意的处理」），`dist/` 在 `.gitignore` 内不进提交

## 8B 导出声明密钥指纹与提示（D14 / D15）

> 第 8 组的 8.4 已经把「跨机恢复要求同一 `ENCRYPTION_KEY`」写成了琥珀色警示，但那是一段散文：运维要真去核对，只能把两台机 `.env` 里的 32 个字符拿出来肉眼比。本组把这件事变成文件里可核对的事实 —— 导出声明指纹，导入比对指纹并提示。**只提示不拦**，与否决二的边界见 D15。

- [x] 8b.1 `encryptionKeyCheckService` 把 `keyFingerprint` 从 `_internal` 提为正式导出（`_internal` 里保留同一引用，测试不受影响）；`backupService` MUST 复用该函数，MUST NOT 另写一份派生 —— salt 一漂移，导出指纹就与启动自检日志里那行不相等，运维会据此误判「密钥被换过」
      —— 已提为正式导出 `module.exports = { check, keyFingerprint, FINGERPRINT_ALGORITHM, _internal: { …, keyFingerprint } }`，`_internal.keyFingerprint` 仍指向同一函数引用（既有 9.20 判定直测不受影响）。新增 `FINGERPRINT_SALT`/`FINGERPRINT_HEX_LEN`/`FINGERPRINT_ALGORITHM` 常量并把派生结果 memoize（`_fingerprintCache`，`'unavailable'` 故意不缓存，改回 `.env` 后不重启即可自愈）。`backupService`/`storage.js` 全部 `require` 这一个函数，无第二处派生。9.24 实测：`/backup/summary`、导出响应头、产物 `metadata.encryption.keyFingerprint`、在跑实例启动自检日志四处**逐字同为** `87389747817f`。
- [x] 8b.2 `exportBackup()` 的 `metadata` 增设 `encryption: { keyFingerprint, algorithm, notice }`：`algorithm` 写明派生方式（`scrypt(ENCRYPTION_KEY, 'relay-key-check-fingerprint', 16)` 截断 12 hex），`notice` 是可读中文提示。MUST NOT 写入 `ENCRYPTION_KEY` 明文或其任何未加盐摘要（尤其 MUST NOT 用 `sha256(KEY)` —— 那是 bedrock 实际使用的 AES 密钥，D12）
      —— 已加。`algorithm` 落地为 `scrypt(ENCRYPTION_KEY, "relay-key-check-fingerprint", 16) 取前 12 位 hex`（复用 `FINGERPRINT_ALGORITHM` 常量，与自检同一字面）。9.29 实测（阳性对照 6/6，产物全文零命中）：产物里既没有 `ENCRYPTION_KEY` 明文，也没有 `sha256(KEY)`（hex/base64）、`md5(KEY)`、`base64(KEY)`、以及未截断的 scrypt 全长任一形态；且 12 位指纹不是 sha256/md5 的前缀（独立 salt 派生）。
- [x] 8b.3 `notice` 文案 MUST 把两类后果分开讲（D14）：账户凭据是可逆密文 → 目标机密钥不同则上游 401，可重新授权/重录；已发放 API Key 是 `sha256(明文 + KEY)` 且明文从不落库 → 目标机密钥不同则在中转入口就 401，**无法**恢复，只能重新发放。只写「需要同一密钥」会让人以为改回密钥即可，而改不回去时这两类的下场不一样
      —— `KEY_NOTICE`（287 字）两类后果分列：①账户凭据解不开→上游 401→可重新授权/重录；②已发放 API Key 哈希也不同→中转入口 401，明文既不在备份也不在库里→无法恢复，只能重新发放；末句点明唯一支持路径（目标机建立数据前把 `ENCRYPTION_KEY` 设为源机值再导入，不提供跨密钥重加密）。9.27 实测：产物 `notice` 同时命中「账户/401/重新授权|重录」与「API Key/重新发放」，判定通过。
- [x] 8b.4 `GET /backup/export` 增响应头 `X-Backup-Key-Fingerprint`（导出是 blob 附件下载，前端不解析 body，body 里的字段对界面不可见）；`GET /backup/summary` 响应增 `encryption: { keyFingerprint, algorithm }`，让面板在**点导出之前**就能显示本机指纹。两处 MUST 都在 `authenticateAdmin` 之后，MUST NOT 出现在任何免鉴权出口
      —— 三处都已落地：导出响应头 `X-Backup-Key-Fingerprint`（同源同域，前端 `resp.headers` 直接可读，无需 CORS expose）、`/backup/summary` 的 `data.encryption`（由 `summarize()` 在存在时附加）、以及**新增的第三处** `/admin/storage/status` 的 `data.encryption`。**与任务原文的偏差（须报备）**：面板运行时的指纹取自 `/storage/status` 而非 `/backup/summary` —— `/backup/summary` 内部要跑一整趟 `exportBackup()`（全量 dump 后只为数数），挂载即拉它等于每次进设置页都全量导出一次；而 `/storage/status` 本就被面板 10s 轮询，顺带带回指纹是零成本。`/backup/summary` 的字段仍按原文加上，供 API 调用方使用。9.30 实测：`/backup/summary`、`/backup/export`、`/storage/status` 三个端点不带 token 均 **401**，响应头无 `x-backup-key-fingerprint`、响应体既无 12 位指纹也无 `keyFingerprint` 字样。
- [x] 8b.5 `importBackup()` 比对指纹，三态且 MUST NOT 阻断导入（D14 / D15）：不同 → `addWarning('encryption-key-fingerprint-mismatch', …)` + `logger.warn`，导入照常写完；相同 → **不产生任何告警**（正常迁移是主流路径，主流路径上多一条告警等于让告警整体失效）；缺失或任一侧为 `'unavailable'` → 只记 info。判据 MUST 是「`metadata.encryption.keyFingerprint` 是否存在且可用」，MUST NOT 由版本号推断（指纹派生失败时 `2.3` 产物同样没有可比对的指纹）
      —— 已按三态实现，判据写作 `!backupKeyFingerprint || 任一侧 === 'unavailable'`（读字段存在性与可用性，全程未读 `metadata.version`）。9.25 / 9.26 / 9.28 三次真实 HTTP 导入实测：①指纹一致 → `warnings` 共 0 条、指纹相关 warn/error 日志 0 行、error 日志增量 0 字节；②指纹改成 `deadbeefcafe` → 恰好 1 条 `encryption-key-fingerprint-mismatch` + 恰好 1 行 warn 日志，HTTP 200 且 `success:true`，四桶计数与一致那次逐项相等（apiKeys 27 / accounts 5 / admins 1）；③整段 `metadata.encryption` 删除（模拟 2.0 / 2.1 / 2.2）→ 恰好 1 条 info「跳过密钥指纹比对」，0 告警、0 warn/error，未被当成不一致。
- [x] 8b.6 告警的 `message` MUST 与 8b.3 的 `notice` 口径一致（同样分开讲两类凭据），`context` 带两侧指纹 —— 指纹是可写日志的，密钥不是
      —— `KEY_MISMATCH_MESSAGE` 与 `KEY_NOTICE` 同口径（都分列「账户凭据解不开→上游 401→可重新授权/重录」与「已发放 API Key 哈希算不出→中转入口 401→无法恢复只能重新发放」），并额外交代「本次导入已照常完成，未因此拦截」。`context` 传 `{ backupKeyFingerprint, localKeyFingerprint }`；注意 `addWarning` 是既有的 `{ type, message, ...context }` **扁平**结构（D11 约定），字段落在告警对象顶层而非 `.context` 下 —— 首轮验证脚本按 `.context` 取值误报了一次 FAIL，是**测试写错**而非实现缺字段，改断言后 30/30 全过。日志侧只打两个 12 位指纹，不打密钥。
- [x] 8b.7 `BACKUP_VERSION` `'2.2'` → `'2.3'`；`backupService.js` 头部注释与 D10 矩阵同步。MUST NOT 因版本号变化在导入侧新增任何 gate（现有分流仍按 `data.groups` / `__type` 的**存在性**判定）
      —— 已改为 `'2.3'`，头部注释补上 v2.3 的 `metadata.encryption` 说明，与 D10 四行矩阵一致。9.24 实测产物 `metadata.version === '2.3'`；导入侧新增代码只有指纹比对（不读 version、不 return、不 throw），9.28 的「删掉 encryption 段但版本仍写 2.3」正是对这一点的反证：版本是 2.3 却照常走「跳过比对」，说明判据不在版本号上。
- [x] 8b.8 `StorageHealthSection.vue`：琥珀色警示框内显示本机指纹（来自 `summary`），导出成功提示带上本次导出所声明的指纹（来自响应头），并补一句「目标机指纹与此不同则账户凭据与已发放 API Key 都不能直接使用」；导入结果的告警块复用既有 `importWarnings` 渲染，无需新组件。指纹缺失时 MUST 优雅退化（少一句话，不显示 `undefined`）
      —— 琥珀框内加了一段 `v-if="keyFingerprint"` 的提示（带 `data-secharden-key-fingerprint` 标记便于验证），指纹由 `computed(() => status.value?.encryption?.keyFingerprint || '')` 从**已在 10s 轮询的** `/storage/status` 派生（来源偏差见 8b.4）；`doExport()` 成功文案改为读响应头 `x-backup-key-fingerprint`，取到则带指纹并点明两类凭据下场，取不到回落原文案。未新增组件，导入告警仍走既有 `importWarnings`。9.31 实测（`@vue/compiler-sfc` 编译仓库里那份真实 SFC + jsdom 真实挂载，只把 `@/utils/http_apis` 换成桩，18/18 通过）：A 有 encryption → 指纹与两类后果齐现；B 老后端无 encryption → 该段整体隐藏、渲染结果无 `undefined`、警示框其余内容不受影响；C 响应头带指纹 → 提示出现「密钥指纹 87389747817f」；D 响应头不带 → 回落为「备份已导出并开始下载」且无 `undefined`。
- [x] 8b.9 导出面板 MUST NOT 因本组新增任何密钥输入控件（8.5 的约束继续有效，重加密已否决，不留半成品入口）
      —— 全文件扫描 `type="password"` / `v-model` 均 0 命中（该组件本就没有任何输入绑定，唯一 input 是既有的隐藏 file input）；`ENCRYPTION_KEY` 只出现在说明文字里，指纹是只读 `<code>` 展示。没有留任何「填入源机密钥」式入口。
- [x] 8b.10 `cd web/admin-spa && npx eslint --fix && npx vite build`
      —— 本地镜像没有 SPA 的 `node_modules`，故在 dev 上跑：`npx eslint --fix src/components/settings/StorageHealthSection.vue` 退出码 0（--fix 顺手重排了提示语的折行，已把格式化后的文件**回拉**到镜像，两侧 md5 同为 `bde4df64f31706da89ff4ea73bb71d07`）；`npx vite build` ✓ built in 16.19s，无报错（仅既有的 chunk >500kB 提示）。

## 9. 验证

> 验证顺序 MUST 是「先 9A（索引与分组，D1-D4 / E1），再 9B（密钥自检，D5a）」：D5 的症状（账户可见但全部 401）会被 D1-D4 的症状（账户完全不可见）掩盖，顺序颠倒会分不清是谁没修好（D13）。

### 9A 索引、分组与 API Key 映射（D1-D4 / E1）

- [x] 9.1 **D1 核心用例（必须在非空实例上做）**：43.110.32.63 上先确保某平台已有 ≥1 个账户（索引非空），再导入含另一个该平台账户的备份 → 新账户 SHALL 立即出现在管理台列表；`smembers <前缀>index` SHALL 含新 id
      —— dev 43.110.32.63 实测（`phase3.js`，21/21 PASS）。前置就是这条用例的关键：导入前 `openai:account:index` 已有 4 个真实成员（非空 → 旧代码的 SCAN 回退路径根本不会触发，正是 D1 的复现条件）。备份里放一个克隆账户（`crypto.randomUUID()` 新 id，强制 `schedulable:'false'` + `isActive:'false'`，不参与真实调度），导入后 **不重启**：`exists openai:account:<新 id>` = 1、`smembers openai:account:index` 含新 id（4 → 5）、`GET /admin/openai-accounts` 返回列表里能查到该 id —— 即「实体写回」与「管理台可见」两件事同时成立。导入 stats 的 `indexes` 段留下了显式凭据：`{"openai:account:index":1,"apikey:idx":1}`（旧代码这里恒为空）。用例结束即用 `cleanup9.js` 把克隆账户软删+永久删除，dev 真实账户数回到 4
- [x] 9.2 对照用例：同一份备份导入空实例 → 同样可见（确认没把原本能走通的 SCAN 回退路径改坏）
      —— `phase4.js` 在 Redis 独立 scratch db（`REDIS_DB=9`，且 `METADATA_BACKEND=redis` 以免 `reconcileAll` 拿空库去对账真实 SQLite）里跑，15/15 PASS，db0 一行没碰，脚本收尾 `flushdb`。空库导入后：账户实体存在、`smembers openai:account:index` 含新 id、`exists openai:account:index:empty` = 0（空标记没被留下，否则后续读取会被 1 小时的假「空」缓存住）、再用管理台列表实际入口 `getAllIdsByIndex('openai:account:index','openai:account:*',/^openai:account:(.+)$/)` 复查同样取到该 id。结论：新增的显式索引登记与原有的 SCAN 回退互不干扰，空实例路径未被改坏
- [x] 9.3 **D2 用例**：导出含活跃 API Key 的备份 → 在目标实例（已有其他 API Key，故 `apikey:hash_map` 存在）导入 → **不重启服务**，用还原的 key 发一次请求 SHALL 鉴权成功
      —— dev 实测（`phase3.js`，21/21 PASS）。造一把专用 key `smoke9-d2-<ts>`，从创建响应里拿到明文（只有创建那一刻拿得到），自算 `d2Hash = sha256(明文 + ENCRYPTION_KEY)`；导出 `2.2` 全量备份后把这把 key 软删+永久删除（实体、`apikey:idx*`、`hash_map` 字段全消失，`GET /api/v1/models` 得 401），再导入那份备份。**不重启**即断言：实体回来了、`hget apikey:hash_map <d2Hash>` = 该 keyId、`apikey:idx:all` / `apikey:set:active` 含该 id、用原明文打 `/api/v1/models` 得 **200**。前置条件成立：导入时目标实例的 `apikey:hash_map` 已有 13 个字段（非空 → 旧代码的 `rebuildHashMap` 不会在启动时兜住，正是 D2 的复现条件）。
      **⚠️ 偏差（这条用例暴露的设计错误，已回改三份文档）**：首跑此用例 FAIL —— 映射为 `null`、请求 401、`hash_map` 字段数 13→13 没变，导入 warnings 里对这把正在还原的 key 报 `hashmap-entity-missing`。根因不在实现走样，而在设计稿本身：`design.md` D7 明写「在 `restoreEntityItems` **之前**把 hash_map 摘出单独处理」，而 `restoreHashMap` 又要 `exists apikey:<keyId>` 复核实体——同一份备份里映射与实体成对出现，先合并映射就等于把每条都判成「实体不存在」而跳过，还原的 key 要等下次重启 `rebuildHashMap` 回填才能用，症状与本次要修的 D2 一模一样，且不报错、不计 errors，纯静默退化。已把 hash_map 合并挪到实体写回**之后**（`backupService.js` 里连注释写死「MUST 排在实体写回之后」），并同步改 `design.md` D7、`specs/backup-restore/spec.md`（加一条顺序不变式 + 导入顺序改为「分组 → 账户 → API Key 实体 → hash_map」）、`tasks.md` 2.1。重启 dev 后复跑 21/21 PASS
- [x] 9.4 **E1 用例**：软删除一个 API Key → 确认 `hash_map` 中映射消失 → `systemctl restart relay-app` → 映射 SHALL 仍然不存在；同时确认 47.89.246.67 式的历史残留（重启前 `hexists` 为 1 的已删 key）在重启后 SHALL 被清理，条数与 info 日志一致
      —— dev 43.110.32.63 实测。为不动任何真实 key，先建两把专用 key：`smoke9-keep`（对照）与 `smoke9-softdelete`。重启前基线（`/tmp/phase1.js` → `/tmp/phase1.json`）：`apikey:hash_map` 共 23 个字段 = 活跃 12 + **已软删 11**（这 11 条就是 dev 上天然存在的 prod 式残留，不是造的），软删 `smoke9-softdelete` 后 `hexists` 立即变 0。`systemctl restart relay-app` 后 `logs/claude-relay-2026-08-19.log` 出现 `🧹 清理了 11 条 hash_map 残留映射: 4ae0d49c…(deleted), …, dd44c24b-fbcc-41ca-8671-802ba3354bfc(deleted)`，条数与基线的 11 条完全对上。重启后核对（`/tmp/phase2.js`，12/12 PASS）：软删 key 的 hash 仍 `hexists=0`（没被 `rebuildHashMap` 回填）；11 条残留一条不剩；字段数 13 = 原 12 活跃 + `smoke9-keep`；逐字段回查实体，13 条全部 `isDeleted != true` 且实体存在（已软删 0 / 实体缺失 0）。功能面：`smoke9-keep` 打 `/api/v1/models` 得 200，`smoke9-softdelete` 得 401 —— 清理只砍死映射、没伤活跃映射
- [x] 9.5 **D2×E1 交叉用例**：取一份「导出时 key 还活着、之后该 key 被软删除」的备份 → 导入 → 该 key 的映射 SHALL NOT 被写回，且 SHALL 出现一条 `hashmap-skipped-deleted` warning
      —— dev 实测（`phase3.js` 后半段）。复用 9.3 那份备份（导出时 `smoke9-d2` 还活着），把已还原的这把 key **软删除**（此时 E1 的 `removeHashMapping` 已把映射摘掉），再导入同一份备份。断言：`hget apikey:hash_map <d2Hash>` 仍为 `null`（映射没被写回）、`stats.hashMap.skippedDeleted` = 1、`imported` = 0，warnings 里有一条 `{"type":"hashmap-skipped-deleted","keyId":"<该 key 的 id>"}`；实体侧同时全部计入 `skipped`（实体已在，按实体语义跳过）。这条正是 proposal 里「D2 与 E1 会互相抵消」的落地验证：导入不得把 E1 刚清掉的死映射又请回来
- [x] 9.6 **E1 恢复路径用例**：软删除 → `restore` → 不重启即用该 key 发请求 SHALL 成功（验证 3.7）
      —— 承接 9.5 的现场：此时 key 处于软删态、`/api/v1/models` 得 401、`hash_map` 无映射。`POST /admin/api-keys/<id>/restore` 后**不重启**立刻用原明文再打一次 `/api/v1/models` 得 **200**，并复核 `hget apikey:hash_map <d2Hash>` = 该 keyId、`apikey:set:active` 含该 id、`apikey:set:deleted` 不含 —— restore 路径把映射与索引族两边都补回来了。收尾把这把 key 软删+永久删除，dev 不留测试残留
- [x] 9.7 **D3 用例**：源实例建分组 + 绑定账户 → 导出 `2.2` → 目标实例（无该分组）导入 → 分组定义、成员集合、反向索引 SHALL 全部还原；账户 SHALL 参与分组调度；后台编辑该账户并保存 SHALL 返回 200
      —— dev 实测（`phase5.js`，29/29 PASS）。现场只碰自建对象：一个 `smoke9-grp-<ts>`（platform=openai）分组 + 一个克隆账户 `smoke9-grp-acct-<ts>`；克隆账户 `accountType='group'`，故 MUST NOT 被共享池选中（`_getAllAvailableAccounts` 只收 `accountType==='shared'` 或缺省），这才敢把它设成 `isActive/schedulable=true`。绑定走真实端点 `PUT /admin/openai-accounts/:id {accountType:'group',groupId}` → 200，成员集合与反向索引同时建起。导出 2.2 后把分组与账户从 Redis 抹掉（模拟目标实例没有这个分组），再导入那份 2.2 的窄化版本：`groups.definitions imported 1 / skipped 2`（skipped 2 是 dev 上原有的两个真实分组，按实体语义跳过）、`members.added 1`、`reverse.added 1`。逐项核对：`account_group:<id>` 与 `account_groups` 成员都回来了、定义字段 `name/platform` 与原值一致、`account_group_members:<id>` 含该账户、`account_groups_reverse:openai:<acct>` 含该分组、账户实体上的 `groupId` **没被剥离**且无 `dangling-group-stripped` 告警（2.2 含分组段，剥离不该触发）。**调度**用 in-process 调 `unifiedOpenAIScheduler.selectAccountFromGroup(groupId)` 与 `selectAccountForApiKey({openaiAccountId:'group:<id>'})` 两个入口各验一次，都选中该账户（日志 `🎯 Selected account from group: smoke9-grp-acct-… priority: 50`）；这条路径只读 Redis、不刷 token、不发上游请求（已读 `_ensureAccountReadyForScheduling` 确认，纯限流/可调度判定）。最后 `PUT` 编辑保存 → 200。顺带验了备份 MUST NOT 含 `account_groups_reverse:migrated`（全文串搜无命中）
- [x] 9.8 **D3b 用例**：用一份真实的 `2.1` 备份导入 → 账户 SHALL 无 `groupId` 字段；SHALL 出现 `dangling-group-stripped` warning；后台编辑保存 SHALL 返回 200
      —— 「真实 2.1」是现场用**改动前的代码**导出的，不是把 2.2 手工降级冒充：`git show HEAD:src/services/backupService.js` 落到 `src/services/backupServiceOld21Tmp.js`（放在 src 内是为了相对 require 能解析），in-process 调它的 `exportBackup()`，确认 `metadata.version === '2.1'` 且无 `data.groups`、且该账户条目确实带着 `groupId`；脚本收尾把这个临时文件删掉并断言不存在（`git status` 复核无残留）。清场后导入这份 2.1 的窄化版本：账户实体写回且进了 `openai:account:index`，`hgetall` 里 **无** `groupId` 字段，warnings 里正好一条 `dangling-group-stripped`，message 带账户名与原 groupId、`accountKey` 指向实体 key。随后 `PUT /admin/openai-accounts/:id` 编辑保存 → 200（旧代码在这里就是 500 的那条路）
- [x] 9.9 **D3c 用例**：手工把某账户 `groupId` 改为不存在的 uuid → 后台编辑保存 SHALL 返回 200 且字段被自动解绑，SHALL NOT 500
      —— 手工 `hset openai:account:<acct> groupId <随机 uuid> accountType group` + `sadd account_groups_reverse:openai:<acct> <随机 uuid>`（反向索引才是管理台读的东西），然后按管理台真实提交的样子把这个已悬空的 gid 原样交回 `PUT`：返回 **200**（不是 500）、反向索引里那个 gid 已被清掉、`account_group_members:<悬空 gid>` 未被建出、同一次保存的 `description` 正常写入；主日志里留下 `⚠️ 账户 <id> 的分组引用已自动解除绑定（分组不存在）: <gid>`（按 mtime 取最新 `claude-relay-*.log` 的增量段抓到，**首跑这条 FAIL 是取证脚本的问题**：日志文件名日期滞后一天，按 `new Date()` 拼路径指向了不存在的文件，改成按 mtime 选文件后通过，实现侧没问题）。
      **如实记录一处边界**：`PUT` 之后账户实体上那条存量 `groupId` 字段**仍是那个悬空 uuid**（`/tmp/phase5.json` 的 `after99`）。没顺手 `hdel` 它是权衡后的选择 —— 共享助手 `pruneDanglingGroupRefs` 只拿得到 `platform`，而 8 类账户实体 key 前缀各不相同，从 platform 反推前缀推错就是往别的 key 上写；而分组归属的权威来源是成员集合：`getAccountGroups()` 遍历 `account_group_members:*`、管理台表单按 `GET /account-groups/:id/members` 回填、调度器走 `getGroupMembers()`，三条读路径都不看实体上的 `groupId`，故该残留字段惰性无害且每次保存都会被再剔除一次。已把这个边界写进 `specs/backup-restore/spec.md`（把「该字段 SHALL 被自动解绑」改成可逐项核对的三件事 + 补一段权威来源与惰性字段的说明）与 `design.md` D8，避免读者按字面以为实体字段会被清掉
- [x] 9.10 **D4 用例**：导入后**不重启**核对 API Key 索引族 —— `scard apikey:idx:all` SHALL 等于 apikey 实体总数、`scard apikey:set:active` SHALL 等于未软删除数、`scard apikey:set:deleted` SHALL 等于已软删除数，且还原的 key SHALL 直接出现在管理台 API Key 列表；`exists apikey:index` SHALL 仍为 0（MUST NOT 被本次改动造出来）
      —— dev db0 实测（`phase6a.js`，**28/28 PASS**，全程走 HTTP + Redis，**没有重启 relay-app**）。现场只碰自建的两个对象：一把 `smoke9-d4-<ts>` API Key 与一个克隆账户 `smoke9-d4-acct-<ts>`（`isActive=false` + `schedulable=false` + `accountType=group` 三重不接流量），跑完即清。流程：建 key → HTTP 导出 2.2 → 裁成只含这把 key 的实体 + 它那一条 `hash_map` 映射 + 那个克隆账户的窄备份 → 软删 + **彻底删除**制造缺口（先断言实体、`hash_map` 映射、`idx:all`/`set:active`/`set:deleted` 三处都已摘净）→ `POST /admin/backup/import`。导入统计 `apiKeys{imported:1,errors:0}`、`accounts{imported:1,errors:0}`、`indexes{"openai:account:index":1,"apikey:idx":1}`。索引族逐项：`idx:all` 28 == SCAN 出的实体 28（成员双向逐个相等，缺 0 孤儿 0）、`set:active` 14 == 未软删 14、`set:deleted` 14 == 已软删 14（都双向比成员，不只比 scard）、三个 zset（`idx:createdAt`/`idx:lastUsedAt`/`idx:name`）也各含还原的 id、`exists apikey:index` 仍为 **0**。管理台侧不重启即可见（`GET /admin/api-keys?pageSize=500` 命中 1 条，`index-status` 报 `ready:true`，说明列表走的是索引快路径而不是退化成全量 SCAN），附带用还原的明文打 `/api/v1/models` 得 200（D2 的回归哨兵）。
      **两处取证方法上的讲究**：① 分母 MUST 用 SCAN 出的 `apikey:<uuid>` 实体数（按 uuid 正则筛掉 `hash_map`/`idx:*`/`set:*`/`tag:*`/`cache:*` 等派生 key），MUST NOT 用管理台返回的条数——管理台读的就是被测索引，拿它当分母等于自证。② 导入后的等式要可归因，前提是导入前就一致，故脚本开头先钉一条基线断言。
      **首跑 FAIL 及其处置（既有脏数据，非本次改动所致）**：第一版跑出 `idx:all 29 vs 实体 28，孤儿 1`、`set:deleted 15 vs 已软删 14`。查得孤儿是 `28e11a0d-…`（`smoke-test-67`，`stdout.log` 显示 2026-08-13 由早期 backup 冒烟建立并**通过管理台软删**，实体后来被某轮手工清理直接删掉、索引条目没跟着走），`idx:all`/`set:deleted`/`idx:createdAt`/`idx:name` 四处都还留着它、`hash_map` 里已无映射。与本次改动无关（导入路径只增不删）。用产品自带的 `POST /admin/api-keys/index-rebuild` 清掉（`totalIndexed` 28 → 27，孤儿归零），复跑即 28/28。这条也反向说明 9.10 的断言必须是**精确相等 + 双向比成员**：只比 scard 或只查「缺」都抓不到这种孤儿
- [x] 9.10b **D4 降级用例**：无索引登记失败时 `apikey:index:version` SHALL 保持原值（列表继续走索引快路径）；构造一次登记失败（如临时改坏 `INDEX_KEYS.ALL_SET` 复核目标）时该 version SHALL 被删除并留下 warn 日志
      —— 成功侧在 `phase6a.js` 里同场验：导入前后 `apikey:index:version` 都是 `2`（未被删），日志里 `dropping apikey:index:version` 命中数 0 → 0（没触发降级）。失败侧在 scratch db9 里做（`phase6b.js`，**19/19 PASS**，`METADATA_BACKEND=redis`，db0 一行不碰）：`set apikey:index:version 2` 后把 `apiKeyIndexService.addToIndex` **打成 no-op** 再导一条 API Key，结果 `apiKeys{imported:1,errors:1}`（失败没被计成成功）、实体仍在（按 D2 决策不回滚）、`idx:all` 里没有该 id、`apikey:index:version` 变为 **null**、日志留下 `backup import: 1 index write(s) failed, dropping apikey:index:version to force a rebuild on next start`（条数写进了消息里），且此刻 `getStatus().ready === false`（查询按设计回退全量 SCAN）。随后恢复真实 `addToIndex` 再导一条作对照：`errors:0`、version 保持 `2`、id 进 `idx:all`。
      **构造方式与任务书原话的偏差（更严的写法，不影响结论）**：任务书举例「临时改坏 `INDEX_KEYS.ALL_SET` 复核目标」，但 `addToIndex` 与写后复核读的是**同一个** `INDEX_KEYS.ALL_SET`，一起改坏两边仍然自洽、测不出东西。改用 no-op 是因为要防的正是这个形态：`addToIndex` 内部把异常吞成 `logger.error` 而不抛，「写失败」在调用方看来与「写成功」完全一样，只有写后 `sismember` 复核能识破；打成抛异常反而绕开了要守的那一行。
      **顺带证到一条**：`apiKeyIndexService` 是单例、redis 句柄由 `app.js` 启动时 `init(redis)` 注入，独立脚本里不 init 时 `this.redis` 为 null，`addToIndex` **直接 return** —— 这就是一个真实存在的静默 no-op 通道（首跑对照用例因此误 FAIL），也再次说明写后复核不是多余的。脚本已补 `init(redis)`，使失败侧与对照侧唯一差别只有那个手工补丁
- [x] 9.11 **索引重建用例**：手工 `srem` 掉某账户的索引条目 + 手工 `sadd` 一个不存在的 id → `del account:index:version` → 重启 → 索引 SHALL 与实体集合完全一致（补齐 + 清孤儿两个方向都验）
      —— dev 埋点（重启前，`phase1.js`）：`srem openai:account:index 4b822bc1-da60-4ed8-a19c-e586a96f4743`（真实存在的账户，制造「实体有、索引无」）+ `sadd openai:account:index ghost-9-11-does-not-exist`（制造「索引有、实体无」）+ `del account:index:version`。重启日志正好一行覆盖两个方向：`openai:account:index: 实体 4，索引 4 → 补写 1，清理孤儿 1`（埋点后索引恰好也是 4 条，但成员错了一个，所以只看条数看不出来，必须比成员）。重启后核对（`phase2.js`）：`4b822bc1…` 已回到索引、`ghost-9-11-does-not-exist` 已消失、`openai:account:index` 与 SCAN 出的 4 个实体 id 集合逐个相等；顺带把 11 个平台前缀全扫了一遍，索引与实体两两一致（claude 1/1、openai 4/4、其余 0/0，无孤儿无缺漏）；`account:index:version` 已回填为 `1`
- [x] 9.12 **兼容用例**：`2.0` / `2.1` / `2.2` 三份备份各导入一次，MUST NOT 报格式错误
      —— `phase4.js` 在 scratch db9 里各导一次（素材都从 dev 真实导出 `/tmp/bk-full.json` 里克隆，字段形态与生产一致）：`2.2`（带 `data.groups`）`imported:1 / errors:0`；`2.1`（无 `groups` 段、账户带一个指向不存在分组的 `groupId`）`imported:1 / errors:0`；`2.0`（无 `__type` 字段）`imported:1 / errors:0` 且账户进了 `claude:account:index`。另补一例 `bedrock_account:*` 的 `__type:'string'` 实体（bedrock 存的是 JSON 字符串而非 hash）：导入后 `type` 仍为 `string` 且 id 进了 `bedrock_account:index` —— 新增的索引登记没把 string 通道带歪
- [x] 9.13 **回滚安全用例**：`2.2` 备份在旧代码实例上导入 SHALL NOT 报错（`data.groups` 被忽略）
      —— scratch db9 实测（`phase6b.js` 后半段）。旧代码同样是现场从 git 取的、不是手写模拟：`git show HEAD:src/services/backupService.js` 落到 `src/services/backupServiceOld21Tmp6b.js`，先断言它的 `BACKUP_VERSION === '2.1'`（新版是 `2.2`）再用它 `importBackup()` 一份**带 `data.groups` 的 2.2 备份**（含 1 个分组定义 + 成员集合 + 反向索引、1 个带 `groupId` 的 openai 账户、1 个 `__type:'string'` 的 bedrock 账户、1 把 API Key）。结果：**没有抛异常**，`accounts{imported:2,errors:0}`、`apiKeys{imported:1,errors:0}`；`keys account_group*` 返回**空**（`data.groups` 被旧代码整段忽略，没造出半个分组 key）；账户与 API Key 实体照旧写入；`bedrock_account:<id>` 的 type 仍是 `string`（`__type` 从 2.1 起就有，2.2 没在这里加新语义）。
      **顺手留一条反向证据**：旧代码导入后 `sismember openai:account:index <id>` 与 `sismember apikey:idx:all <id>` 都是 **0** —— 这正是 D1 的原始症状（数据在 Redis、管理台看不见），也就是万一回滚到旧版本后会回到的样子，可作升级说明里「回滚代价」的实测依据。临时文件跑完即 `unlink` 并断言不存在，`git status --short src/services` 复核无 `backupServiceOld21Tmp` 残留
- [x] 9.14 **幂等用例**：同一份 `2.2` 备份连续导入两次 → 第二次全部计入 skipped，索引与成员集合 SHALL 无变化
      —— `phase4.js`：第一次导入后对 db9 做全库快照（`keys *` 逐个按 set/hash/zset/string 取成员或字段名），第二次导入同一份备份后再取一次快照。结果：第二次 `accounts` = `imported 0 / skipped 1 / errors 0`；`stats.indexes` 为**空对象**（被跳过的实体不触发索引补写——否则会掩盖真正的索引登记失败）；两次快照逐 key 比对**零差异**，索引集合与成员集合都没动
- [x] 9.15 sqlite backend 下确认 `metadataSync.reconcileAll()` 仍在导入末尾成功触发，账户与 API Key SHALL 立即落 SQLite（既有 spec 要求，不得被本次改动破坏）
      —— 与 9.10 同场（`phase6a.js`，dev db0，`.env` 里 `METADATA_BACKEND=sqlite`、`SQLITE_PATH=./data/metadata.db`）。导入返回后**不重启、不等定时轮**，直接用 `better-sqlite3` **只读**打开 `data/metadata.db`（只读是为了不与在跑的服务抢写锁）查两张表：`api_keys` 里有还原的那把 key（`name=smoke9-d4-<ts>`、`status=active`，1 行），`accounts` 里有还原的那个账户（`platform=openai`、`name=smoke9-d4-acct-<ts>`，1 行）—— 说明导入末尾的对账确实把 Redis 的新数据推到了 SQLite。
      **日志证据的取法**：`metadataSync.reconcileAll() completed` 这句在库里本来就有历史命中（定时轮与前几轮验证留下的），所以断言写成「导入前 13 次 → 导入后 14 次，**新增 1 条**」，而不是「命中数 > 0」——后者拿历史日志就能蒙过去

### 9B 启动期密钥自检（D5a）

- [x] 9.16 **前置**：`cp .env .env.bak` 后临时把 `ENCRYPTION_KEY` 改成另一个 32 字符值 —— dev 与 prod 当前指纹恰好一致（按本服务自己的 salt `relay-key-check-fingerprint` 现场从两台机器各自的 `.env` 派生，均为 `87389747817f`；此前记的 `8517dcae95ce` 是早期临时脚本用别的派生方式算的，作废），不动配置复现不出失配
      —— dev db0 实测（`phase8.js`，**24/24 PASS**，含三次真实 `systemctl restart relay-app`）。这台机上有真实用户流量（同一时段能看到 `claude-opus-5` 的 200），所以整个改配置动作被压成一个脚本里的「换 → 重启 → 取证 → 还原 → 重启」闭环，**失配窗口只有 2 秒**（error 行 `15:02:21`，还原后的 info 行 `15:02:23`），且还原写在 `finally` 里、顶层 `catch` 里再兜一次，任何路径抛错都不会把错密钥留在盘上。改法本身也逐项钉住：替换前先断言 `ENCRYPTION_KEY=` 在 `.env` 里**只命中 1 行**（不唯一就抛），替换后行数不变、md5 变、`diff .env.bak .env | grep -c "^[<>]"` **恰好为 2**（只有这一行不同）、指纹 `87389747817f` → `bd829b3d206e`（错密钥用 `phase8MismatchKeyDoNotUse` 补到 32 字符）。指纹在**子进程**里算：父进程的 `config` 是 require 时的快照，改完 `.env` 再在同一进程里读只会拿到旧值
- [x] 9.17 **失配用例**：重启 → 日志 SHALL 出现 error 明示当前 `ENCRYPTION_KEY` 与库中已有密文不匹配，并列出受影响平台与抽样失败数；同时 `/health` SHALL 返回 200，服务 SHALL 正常起来（验证 MUST NOT 阻断启动）
      —— 错密钥重启后 error 行原文（`"lvl":"error"`，日志文件里 emoji 已被剥掉）：`ENCRYPTION_KEY 与库中已有密文不匹配：openai(失败 4 条: openaiOauth,openaiOauth,openaiOauth,openaiOauth)。当前密钥指纹 bd829b3d206e。这些账户会表现为「可见但上游 401」。跨机迁移 MUST 在建立任何数据之前把 ENCRYPTION_KEY 设为源机的值，本次不支持跨密钥重加密。 另有 claude 各仅 1 条密文且同样未解出，样本不足以单独定论但指向同一结论。` —— 受影响平台、抽样失败数、失败字段名、当前指纹（且是错密钥那个 `bd829b3d206e`，不是原值）四样都在，并且同一条也落进了 error 专用日志文件（增量 2381B，值班只看 error 文件也能发现）。「MUST NOT 阻断启动」验的是服务真活着而不只是端口开着：`/health` 200（`readyMs=947`）、管理台 `POST /web/auth/login` 登录成功、`GET /admin/accounts` 仍列出 4 个 openai 账户 —— 这正是 D5a 要提前喊出来的那个症状「可见但上游 401」。另外通读该窗口日志，错密钥与真密钥的明文**都没有**出现在任何一行。
      **顺带守住「自检只读」**：换密钥前 / 失配窗口内 / 还原后各做一次 11 类账户前缀的**摘要快照**（`snap.js`，每个字段只存 `sha256(value).slice(0,16)`，bedrock 整串存 `__whole`，快照文件本身因此不会变成一份凭据副本），两次比对**零差异** —— 错密钥启动没有把任何凭据字段重写坏（这是最坏情况：万一实现里有「解不开就重新加密回写」的逻辑，数据就永久损坏了，所以这条必须用数据本身而不是日志来证）
- [x] 9.18 **恢复用例**：还原 `.env` → 重启 → SHALL 只有 info 级通过日志、SHALL NOT 有 error/warn；并用某账户发一次真实上游请求确认凭据恢复可用（这一步 MUST 做，仅看日志不足以证明密钥真的对上了）
      —— 还原后 `.env` 与 `.env.bak` md5 逐字节一致、指纹回到 `87389747817f`、日志只有一条 `"lvl":"info"` 的 `self-check passed (抽样 2 条，平台 claude、openai，93ms，指纹 87389747817f)`，本次新增段里自检相关行共 130B 且**没有** error/warn，error 日志文件增量 **0 字节**。真实上游请求（`upstream9.js`）：临时建 key → `POST /api/v1/messages`（`model=claude-opus-5`，dev 真实流量用的就是这个别名）→ **200**，响应是 SSE（`content-type: text/event-stream`），流里有真实回答 `Pong! What can I help you with?` 与真实用量 `input_tokens:122 / output_tokens:2`，跑完把 key 软删 + 彻底删除。这一步的意义就在于它是唯一能区分「日志说对上了」和「凭据真能解密并被上游接受」的证据：密钥没对上时这里会是 **401**，而不是 200。
      **首跑 FAIL 及归因（取证脚本的问题，不是凭据问题）**：第一版拿到 **500** `No available Claude accounts support the requested model: claude-opus-5`，一度误以为是模型别名写错。查账户 hash 才定位到真因：dev 上唯一的 claude 账户 `81d8c79b-…`（`dipin-claude-5x`）是 `accountType=group`，**共享池选号根本看不到它**，而我建的临时 key 没绑任何账户；现存可用的 `langshan-1` 绑的是 `claudeAccountId=group:e475f4f9-084f-4361-80b6-f69510bafc35`。临时 key 照这个形态绑同一分组后即 200。也就是说 500 是「选不到号」，与解密无关 —— 这条边界值得记下来：**只看 HTTP 是否非 200 会误判**，判据必须细到「401 才指向密钥，500 selection 类错误指向绑定/调度」
- [x] 9.19 **无误报用例**：在无账户（或密文字段全空）的实例上启动 → SHALL 记「无法判定」info，MUST NOT 报为不一致（验证 7.8）
      —— scratch db9 in-process 实测（`phase7.js`，**37/37 PASS**，不重启、`.env` 一行不动）。为什么不在 db0 上做：9.19 要的前提是「库里没有任何可抽样的密文」，dev db0 有真实账户，这个前提在那儿根本造不出来；而 `check()` 的行为只取决于库内容，与启动序列无关，故 in-process 调用即可，还省掉两次重启。空库上 `check()` 返回 `verdict='inconclusive'`、`sampled=0`、`okCount=0`、`failCount=0`、`platforms=[]`，日志只有一条 info：`ENCRYPTION_KEY self-check skipped：库中暂无可抽样的密文字段（新装实例的正常状态）`，且本次新增日志段里自检自己的行**没有任何 error/warn**、error 日志文件增量里也不含自检字样。顺带钉住只读约束：跑前种进去的 key 集合与跑后逐个相等，没多出 `:empty`（取 id 用 `smembers` 而不是 `getAllIdsByIndex()`，后者在索引空时会 `setex :empty`）
- [x] 9.20 **legacy 形态用例**：手工造一个不含 `iv:` 前缀的裸 hex 字段 → SHALL 单独出 warn，MUST NOT 被计入密钥不一致的证据（验证 7.7）
      —— 同场（`phase7.js` 后半段）。手工种一个 `claude_console_account:<uuid>`，`apiKey` 是 64 位裸 hex（无 `iv:` 前缀）并登记进索引；**同时**从 db0 只读克隆一个真实 claude 账户进 db9（密文是 dev 本机密钥加的，先断言其形态确为 `cipher` 而不是明文历史数据）。这样整体判定落在 `consistent` 上，「legacy 没被算成失配证据」才是硬结论 —— 只种 legacy 的话库里没有任何成功样本，判定天然是 `inconclusive`，等于拿「判不出来」冒充「没误报」。实测：`legacy=[{platform:'claudeConsole',field:'apiKey'}]`（1 处）、`failCount=0`、`verdict='consistent'`、`platforms` 里**只有** `claude:consistent`（legacy 所在平台一条证据都不提供，不进 platforms），日志是两行分开的 —— 密钥判定那行 `"lvl":"info"` 的 `self-check passed (抽样 1 条，平台 claude，35ms，指纹 87389747817f)`，legacy 那行 `"lvl":"warn"` 的 `发现 1 处 legacy 加密形态（裸 hex、无 iv 前缀）：claudeConsole.apiKey`，error 日志文件增量为 0 字节
      **判定逻辑直测（这服务唯一容易写错的地方，14 条）**：`classifyCipher` 七形态各一条（空/undefined→`empty`、`iv:密文`→`cipher`、64 位与刚好 32 位裸 hex→`legacy`、`sk-` 明文→`plaintext`、31 位裸 hex→`plaintext`，即 legacy 阈值边界）；`judgePlain` 七条，其中最要紧的是「解密失败原样返回入参 → 判 fail」（两条解密路径失败时都 `return` 入参，这是 catch 抓不到的那个坑）以及 json 探针对乱码与「合法 JSON 但不是对象」都判 fail。另外 `keyFingerprint()` 为 12 位 hex，且**不是** `sha256(ENCRYPTION_KEY)` 的任何前缀（那正是 bedrock 实际使用的 AES 密钥），并与在跑的 relay-app 上次启动时自己打出的指纹逐字相等 `87389747817f`（同密钥同 salt 必然同指纹，用它替掉了原先写死的常量）
      **两处取证方法上的坑（首跑 4 条假 FAIL，实现侧无问题）**：① winston 的文件 transport 是**异步落盘**的，`check()` 返回那一瞬间日志行还没进文件，第一版读一次就断言，结果三条日志断言全空 —— 改成轮询等待（上限 8s）。② 日志文件里的行是 JSON（`{"ts":..,"lvl":..,"msg":..}`）**且 emoji 被剥掉**，所以断言 MUST NOT 拿 `🔑` 去匹配文件内容，级别也只能读 `"lvl":"info"` / `"lvl":"warn"` 字段，不能像看控制台那样按前缀猜。③ 顺带作废一个记错的常量：此前记的 dev 指纹 `8517dcae95ce` 是早期临时脚本用别的派生方式算的；按本服务的 salt 现场从两台机器 `.env` 各自派生，dev 与 prod 都是 `87389747817f`（两机确实同一把密钥，9.16 的前提成立）
- [x] 9.21 **不泄露用例**：通读自检产生的全部日志，`ENCRYPTION_KEY` 明文、密文原值、解密所得明文 SHALL NOT 出现在任何一行（验证 7.10 / 7.11）
      —— dev 实测（`phase9.js`，**9/9 PASS**，只读）。needle 一共 **62 条**，三类都齐：① 密钥相关 4 条（`ENCRYPTION_KEY` 明文、`sha256(key)`（bedrock 实际用的 AES 密钥）、`md5(key)`、`base64(key)`）；② 密文 30 条（11 类账户前缀现存 **15 个密文字段**的原值，外加每条密文去掉 `iv:` 后的密文体 —— 只泄 iv 或只泄密文体同样不可接受）；③ 明文 29 条（那 15 个字段**逐个真解密**所得的明文，JSON 明文再拆出里面每个字符串字段，`accessToken` / `refreshToken` / `idToken` 才是真正的秘密）。needle 过滤掉长度 < 16 与含空白的串，否则 `true`、scope 描述那种词会把断言变成噪声。语料是 `logs/` 下**全部 56 个文件 / 1594MB / 179 万行**（含 `.gz` 历史归档，按行流式扫），从里面筛出自检产生的 **31 行**，逐条比对 62 needle × 31 行 —— **零命中**，且按三类各自单独断言一次（4 / 30 / 29 条全部未出现），便于失败时定位是哪一类漏了。
      **两条让「没搜到」站得住的前提**：① **阳性对照** —— 把 62 条 needle 各注入一段假日志，确认匹配器全部抓到（漏抓 0），否则「没搜到」可能只是匹配器写错了；② 语料**非空且确实是自检那些行** —— 31 行里有 18 行含密钥指纹 `87389747817f`。少了这两条，本用例就是一句空话。本脚本自己也不打印任何 needle（只输出标签与长度），也**不把 needle 落盘**（因此不能用 `grep -F -f needle文件` 那种快路子）—— 查泄露的脚本不能自己变成泄露源。
      **附带查出一处既有泄露（不在本次改动范围，须单独上报）**：把搜索面从「自检行」放大到整个 `logs/` 后，出现 **61 处**命中，全部可归因到**既有的 HTTP 访问日志**（该 logger 把 `req` / `res` 整个 body 打进日志），两种形态：`POST /admin/backup/import` 的**请求体**里带账户密文；`PUT /admin/openai-accounts/:id` 的**响应体**里带**已解密**的 `openaiOauth`（`idToken` / `accessToken` / `refreshToken` 明文），共 8 处。`stdout.log`（nohup 时代的控制台输出）里那 42 处是同一机制的多行形态 —— needle 落在 `├─ req:` / `├─ res:` 续行上，那种行没有 `msg` 字段，所以第一版按 JSON 形态归因时被算成「无法归因 42 处」而 FAIL，补上控制台续行形态后归因率 61/61。这条是产品既有行为（管理台 API 响应本就返回解密后的 token，加上访问日志全量打 body），**不是自检打的、也不是本次改动引入的**，故写成「命中 MUST 全部可归因到访问日志」的断言而不是笼统说一句「与本次改动无关」；本次触发它的流量恰好是我自己的验证请求（`generatedBy:"smoke9"`、`smoke9 phase5 编辑`）
- [x] 9.22 **启动耗时用例**：对比自检前后的启动耗时，MUST NOT 出现可感知的增加（抽样上限生效，验证 7.2）
      —— 与 9.16~9.18 同场（`phase8.js`）。三次真实重启的 **time-to-ready**（`systemctl restart` 起算，毫秒级轮询 `/health` 直到 200）：控制组（正确密钥）**955ms**、失配组（自检活最多，第一轮拿到失败会触发追加抽样）**947ms**、还原后 **947ms**，极差 **8ms** —— 落在同一噪声带里，失配组甚至还略快，说明自检不在启动关键路径上。自检自身净耗时（日志里那行自己报的）**233 / 38 / 31ms**（首次含 scrypt 密钥派生与各平台服务首次 require，后续走缓存），量级与 `SAMPLE_LIMIT=3`、`EXTENDED_LIMIT=8` 的抽样上限相符 —— 若退化成全量解密，dev 上 15 个密文字段就该是秒级。
      **取证难点与绕法（如实记录）**：本用例字面要求「对比自检前后」，但**拿不到改动前的基线** —— `journalctl -u relay-app` 只记 systemd 自己的行（8/17 以来仅 2 次启动，都是新代码），app 日志是秒级精度，减不出毫秒差。故把比较对象换成**同一份代码的三次重启**（其中失配组就是最坏情况：追加抽样 + 全部解密失败），再补一条更硬的结构性证据：按**日志文件字节偏移**核对自检那行排在 `"type":"startup"`（服务已监听）**之后**（startup@1308 vs selfcheck@8230）—— 自检是 `app.js` 里 fire-and-forget 调起的，即使它耗时再长也不会推迟服务可用时间。这比「两个数差得不多」更能说明问题
- [x] 9.23 `npx eslint --fix src/services/backupService.js src/services/apiKeyIndexService.js src/services/accountIndexService.js src/services/encryptionKeyCheckService.js`
      —— 在 dev 上跑 `npx eslint`（先 --fix 再无 --fix 复跑）对上述四个文件外加 `src/routes/admin/backup.js`、`src/routes/admin/storage.js` 一并检查，均 **exit 0**，`--fix` 未产生任何字节变化（md5 前后一致）。

### 9C 导出指纹声明与导入比对（D14 / D15）

- [x] 9.24 **三处指纹逐字相等**：`GET /backup/summary` 的 `encryption.keyFingerprint`、导出响应头 `X-Backup-Key-Fingerprint`、产物 `metadata.encryption.keyFingerprint`，三者 SHALL 与在跑实例上次启动时自检日志里那行的指纹逐字相同（验证 8b.1 的「复用而非另写」—— 这是本组唯一会被静默写错的地方）
      —— `phase10.js` 从 dev 当天 app 日志抓出最近一行自检指纹（`87389747817f`），再依次拉三个来源：`/backup/summary` → `87389747817f` ✓、`X-Backup-Key-Fingerprint` 响应头 → `87389747817f` ✓、导出产物 `metadata.encryption.keyFingerprint` → `87389747817f` ✓。四值逐字相等，另断言 `algorithm` 里含 `scrypt` 且不含 `sha256` 字样、`metadata.version === '2.3'`、`notice` 长度 287 字。
- [x] 9.25 **指纹一致的导入无噪音**：把 dev 自己导出的备份原样导回 → `warnings` 里 SHALL NOT 出现 `encryption-key-fingerprint-mismatch`，日志 SHALL NOT 因比对产生 warn/error（验证 8b.5 的噪音控制）
      —— HTTP 200 且 `success:true`，`warnings` 共 0 条；日志文件按 offset 读增量、拿到 `backup import` 行后统计：指纹相关日志 0 行（全部 info），error 日志增量 0 字节。基准桶量记为 `apiKeys=27 accounts=5 admins=1` 供 9.26 比对。
- [x] 9.26 **指纹不一致的导入照常完成**：手工把产物的 `metadata.encryption.keyFingerprint` 改成另一个值再导入 → SHALL 出现一条 `encryption-key-fingerprint-mismatch` 告警 + 一条 warn 日志；导入 SHALL 照常写完（四桶计数与 9.25 那次同量级），MUST NOT 因此拒绝（验证 8b.5 / D15 的「只提示不拦」）
      —— 把 `keyFingerprint` 篡改为 `deadbeefcafe` 再 POST：HTTP 200 且 `success:true`，`warnings` 里**恰好 1 条** `encryption-key-fingerprint-mismatch`（告警扁平结构里 `backupKeyFingerprint === 'deadbeefcafe'` && `localKeyFingerprint === '87389747817f'`）；app 日志**恰好 1 行** warn 且未升级为 error；四桶计数与 9.25 逐项相等（27 / 5 / 1）。**关于首轮 FAIL 的复盘**：初版脚本按 `mw.context?.…` 取值，与既有 `addWarning` 的扁平结构（`{ type, message, ...context }`，D11 约定）不符 —— 是**测试写错**而非实现缺字段，改成 `{ ...(mw.context||{}), ...mw }` 兼容取值后 30/30 全过。
- [x] 9.27 **告警文案覆盖两类凭据**：9.26 的告警 `message` 与产物 `notice` SHALL 同时提到「账户凭据上游 401 可重新授权」与「已发放 API Key 只能重新发放」，MUST NOT 只写「需要同一 `ENCRYPTION_KEY`」（验证 8b.3 / 8b.6）
      —— 用 `/账户/.test(t) && /401/.test(t) && /(重新授权|重新录入)/.test(t) && /API Key/i.test(t) && /重新发放/.test(t)` 同时匹配 `notice` 与 `mw.message`：两处都命中；且两处都出现「无法恢复|不可恢复|只能重新发放」中的至少一个（口径一致，都点明 API Key 不可恢复）。
- [x] 9.28 **旧备份不产噪音**：删掉产物里整个 `metadata.encryption` 段（模拟 `2.0`/`2.1`/`2.2`）再导入 → SHALL 只有 info 记录跳过比对，MUST NOT 产生告警、MUST NOT 报为不一致（验证 8b.5 第三态）
      —— `delete legacy.metadata.encryption` 后 POST：`warnings` 中无 `encryption-key-fingerprint-mismatch`，app 日志按 offset 读增量后**恰好 1 条** info「跳过密钥指纹比对」，无任何含「指纹」的 warn/error，error 日志增量 0 字节。（顺带反证 8b.7：产物 `metadata.version` 仍是 `'2.3'`，走的却是「跳过」分支 —— 判据在字段存在性，不在版本号。）
- [x] 9.29 **不泄露**：产物全文与本组产生的日志 SHALL NOT 含 `ENCRYPTION_KEY` 明文、`sha256(KEY)`、`md5(KEY)`、`base64(KEY)` 任一形态（沿用 9.21 的 needle 构造与阳性对照做法，MUST 同样不落盘 needle）
      —— 从 `config.security.encryptionKey` 派生 6 条 needle：明文、`sha256(KEY)` hex、`sha256(KEY)` base64、`md5(KEY)`、`base64(KEY)`、`scrypt` 全长（未截断，与本设计所选 12 hex 前缀对照）；阳性对照 6/6 全部被匹配器抓到（漏抓 0）。产物全文（导出的原始 JSON 字符串）**零命中**，本组三次导入增量日志（14 行）**零命中**；额外断言 12 位指纹 `87389747817f` 不是 `sha256(KEY)` / `md5(KEY)` 任一形态的前缀（独立 salt 派生已隔断）。脚本自身**只打印 needle 标签与长度**，且**不落盘 needle**，与 9.21 同款自约束。
- [x] 9.30 **鉴权边界**：不带 token 请求 `/admin/backup/summary` 与 `/admin/backup/export` → SHALL 401，指纹 MUST NOT 出现在响应体或响应头中（验证 8b.4）
      —— 三个端点各测一次（`/admin/backup/summary`、`/admin/backup/export`、以及本次新增的第三处 `/admin/storage/status`），全部 HTTP **401**；响应头 `x-backup-key-fingerprint` **不存在**；响应体既无 12 位指纹 `87389747817f`、也无 `keyFingerprint` 字样，body 就是标准的 `{"error":"Missing admin token","message":"Please provide an admin token"}`。
- [x] 9.31 **前端**：面板在导出前 SHALL 显示本机指纹；导出成功提示 SHALL 带本次声明的指纹；`summary` 缺 `encryption` 段（老后端）时 SHALL 优雅少一句话而不是显示 `undefined`（验证 8b.8）
      —— `fe31.mjs`：用 `@vue/compiler-sfc` 编译**仓库里那份真实 SFC**（不是重写脚本，仅把 `@/utils/http_apis` 换成 stub 桩），jsdom 真实挂载 + Vue 完整运行时。四场景 18/18 通过：**A** `status.encryption.keyFingerprint = 87389747817f` → 琥珀框内出现「本机密钥指纹」段（带 `data-secharden-key-fingerprint` 锚点），指纹本体、账户 401+重新授权|重录、API Key 只能重新发放，三个断言齐现；**B** 老后端 `status` 无 encryption → 该 `<p>` 因 `v-if` 整段隐藏，全渲染无 `undefined`，警示框其余内容（含 `ENCRYPTION_KEY` 术语解释）不受影响；**C** 导出响应头 `x-backup-key-fingerprint: 87389747817f` → 点导出按钮后提示更新为「备份已导出并开始下载（密钥指纹 87389747817f）。恢复到指纹不同的机器上：账户凭据解不开，API Key 只能重新发放。」；**D** 老后端不带该头 → 回落原文案「备份已导出并开始下载」，无 `undefined`。**取证说明**：来源与 8b.4 一致（`/storage/status` 而非 `/backup/summary`）。
- [x] 9.32 `npx eslint --fix src/services/backupService.js src/services/encryptionKeyCheckService.js src/routes/admin/backup.js`
      —— 与 9.23 并入同一次 dev 运行：三个文件加上 `src/routes/admin/storage.js` 一并跑 `npx eslint`（先 --fix 再无 --fix 复跑），**exit 0**，`--fix` 未产生任何字节变化。SPA 侧的 `StorageHealthSection.vue` 已在 8b.10 里单独 --fix 并 build 过（回拉后镜像/dev 两侧 md5 一致）。

## 10. 发布

- [x] 10.1 `npm run version:bump -- 1.3.3` 同步三处版本号（`package.json` / `package-lock.json` 两处 / `VERSION`）。**实际发布偏离本工件原规划**：原定 **minor 1.4.0**（理由：备份格式 `2.1` → `2.3` 属功能新增，对齐 `1.2.11` → `1.3.0` 惯例），实际按用户决策与 `add-openai-account-connectivity-test` **合并发布为 patch 1.3.3**，延续仓库多数功能性变更走 patch 位递增的惯例（`1.2.3`→`1.2.5`→`1.2.9`→`1.2.11`→`1.3.1`→`1.3.2`）。已按要求走脚本同步，未手工单独编辑三处中的任何一处
- [x] 10.2 提交、推同名分支 `v1.3.3`、`git push origin refs/tags/v1.3.3`（MUST 用 `refs/tags/*` 全路径，同名分支会让 `git rev-parse` ambiguous）。本变更与 `add-openai-account-connectivity-test` 合并在同一次发布中推出
- [ ] 10.3 43.110.32.63 上走一次管理台一键升级验证升级路径本身未被破坏
- [ ] 10.4 47.89.246.67 升级前 MUST 先 `/admin/backup/export` 留一份当前备份
- [ ] 10.5 47.89.246.67 升级后核对：16 条已删 key 的残留映射已清理、`apikey:set:active` 与 `apikey:set:deleted` 的数量分别与活跃 / 回收站 key 数一致、5 个 openai 账户仍可见且 `openai:account:index` 与实体一致
- [ ] 10.6 47.89.246.67 升级后 MUST 确认启动日志里密钥自检为 info 通过（该机密文本就由本机密钥加密，出现 error 即说明自检有误报，MUST 当场排查而不是放行）
- [ ] 10.7 升级说明 MUST 写明跨机迁移的唯一支持路径：目标机在建立任何数据之前把 `ENCRYPTION_KEY` 设为源机的值，然后导入备份；不支持跨密钥重加密
