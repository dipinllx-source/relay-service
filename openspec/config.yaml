schema: spec-driven

# 项目上下文（可选）
# 此内容将在创建产物时展示给 AI。
# 添加你的技术栈、规范、风格指南、领域知识等。
# 示例：
#   context: |
#     技术栈：TypeScript、React、Node.js
#     我们使用 Conventional Commits 提交规范
#     领域：电商平台

# 产物级规则（可选）
# 为特定产物添加自定义规则。
# 示例：
#   rules:
#     proposal:
#       - 提案需控制在 500 字以内
#       - 必须包含 "非目标" 章节
#     tasks:
#       - 将任务拆分为不超过 2 小时的粒度
