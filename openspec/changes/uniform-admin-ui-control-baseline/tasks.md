## 1. 样式来源唯一化（不改页面，视觉应保持不变）

- [x] 1.1 移除 `assets/styles/global.css` 中 247 行与 423 行两处 `.btn` 的重复定义，保留唯一一处
- [x] 1.2 移除 `global.css` 中 324 行与 499 行两处 `.form-input` 的重复定义，保留唯一一处
- [x] 1.3 排查并移除 `global.css` 中 `.card`、`.stat-card` 及其他与 `components.css` 重叠的重复定义段
- [x] 1.4 明确 `main.css` 与 `global.css` 的职责边界，确保每个全局类只有一处定义
- [x] 1.5 验证：检索确认 `.btn`、`.form-input`、`.card`、`.stat-card` 各自只出现一处定义
- [x] 1.6 验证：构建后比对看板、API Keys、账户管理、设置四页视觉，确认按钮与输入框表现与去重前一致

## 2. 基础控件类补齐尺寸规格（不改页面，视觉应保持不变）

- [x] 2.1 从 `LandingView` 与 `StartView` 的 scoped `.btn` 中提取完整内边距与字号规格作为基线取值
- [x] 2.2 在 `components.css` 为 `.btn` 增加 sm / md / lg 三档尺寸变体，各档明确 height、padding、font-size
- [x] 2.3 在 `components.css` 确立卡片基线：统一圆角与内边距区间
- [x] 2.4 确立表格基线：表头与单元格使用同一内边距，数值列右对齐并启用等宽数字
- [x] 2.5 确立开关基线：单一尺寸与单一选中色
- [x] 2.6 验证：仅声明基础类与尺寸变体的按钮渲染出规定高度与内边距，三档高度互不相同

## 3. 逐页替换：管理页（按页分批，每批独立验证）

- [x] 3.1 看板页：控件替换为基础类；标题与工具条同行；余额区块改为按是否存在余额语义账户条件渲染；二级指标压为单行摘要；RPM 与 TPM 合并为双栏卡
  - 补充（后续复核）：此前只落地了「余额区块条件渲染」与「标题与工具条同行」，
    「二级指标压为单行摘要」与「RPM/TPM 合并双栏卡」均未做——8 张 stat-card 仍分两排铺开，
    其中「总API Keys」「服务账户」只有一个数字、下半张卡空着。已补：一级指标收为 4 张同密度卡，
    RPM 与 TPM 合成双栏卡带分隔线，API Keys 数 / 账户数 / 累计请求 / Token / 费用 压成 42px 摘要行。
    日期选择器 wrapper 高度写死 32px（size="default" 内部 wrapper 会被内容顶高，差 2~6px）。
    页面总高实测 2100→1508px。
- [x] 3.2 看板页：四个图表统一为 HTML 标题左对齐加右侧灰字附注、图例统一置底；「共 N 个」类元信息并入标题行
  - 补充（后续复核）：标题与附注此前已落地；改造稿另有「底部 3 张全宽图改 2 列 + 1 全宽、
    高度从 438 降到 300 上下」一项未做，已补为 2 列 + 1 全宽、高度收到 260 上下。
  - 顺带修掉一处新引入的口径错误：「近 7 日日均请求」最初复用了 trendData，而后者跟随图表区
    日期筛选（默认预设「今日」只有当天按小时数据），会显示为 0 且随用户切换筛选而变。
    已改为独立取固定 7 天按天趋势，实测切到「30天」后该值保持不变。
- [x] 3.3 API Keys 页：控件替换；标签、限制、过期时间、状态四列降级为名称下方次要信息并在偏离默认时强调；数值列右对齐可排序；操作按钮分主次
  - 补充（后续复核）：初次实现漏了「操作按钮分主次」与筛选/动作分行。已补：
    标题行右侧常驻「刷新 / 更多 / 创建新 Key」，筛选单独成行且两行右缘对齐（实测同为 1375px），
    选择 / 导出 / 管理标签 / 批量编辑 / 批量删除收进「更多」下拉。
- [x] 3.4 API Keys 页：修正展开行跨列数与实际列数一致；修正加载态与空态同时渲染
- [x] 3.5 账户管理页：控件替换并确保图标与文字同行不折行；状态、优先级、代理、到期时间四列降级；余额列移除并将成本并入今日使用
  - 补充（后续复核）：动作区原为 6 个按钮平铺。已收敛为「统计 / 刷新 / 更多 / 添加账户」，
    选择与分组进「更多」；「刷新余额」改为无按量计费账户时不渲染（原先恒渲染并置灰）。
- [x] 3.6 账户管理页：修正状态列表头与单元格宽度约束冲突；分页移入卡片内；移除读取不存在字段的移动端代理行
- [x] 3.7 请求明细页：为工具区弹性容器补居中对齐，消除按钮被纵向拉伸；筛选收为单行；移除无语义的装饰发光样式
  - 补充（后续复核）：装饰发光此前只删了按钮上的，筛选器上仍留 7 处 toolbar-control-glow，
    且按钮走的是自定义 toolbar-action-button（非 btn-md）。已全部移除并统一 btn-md；
    导出 CSV 与清理历史预览收进「更多」；el-select wrapper 因内容顶高比 el-input 高 2px，
    改为写死 height 后筛选器实测统一 32px。
- [x] 3.8 请求明细页：表头与单元格内边距统一；缓存创建与读取合并为一列；推理列改为按需开启
- [x] 3.9 设置页：开关统一尺寸与选中色并移除手写模拟开关的实现；输入框补齐内边距并统一高度；保存与重置成对置于标题行右侧
  - 补充（后续复核）：开关（实测全页 44×24 一种）、输入框（32px）、品牌段保存/重置均已落地；
    但「单一保存范式」只做了品牌设置一段，通知设置仍是 4 处 @change 直接落库
    （改一个开关发一次请求），两套心智模型并存。已补：通知设置改为脏值提示 + 显式保存，
    并补「放弃更改」次级按钮。平台增删/启停仍走独立接口即时生效，但刷新改为只更新
    platforms 列表，否则整体重载会冲掉用户尚未保存的通知类型改动。
- [x] 3.10 设置页：桌面与移动双模板合并为响应式单套；三处「最后更新」信息块合并为一处
- [x] 3.11 使用记录页：数值列改右对齐并启用等宽数字与排序；四张汇总卡压为单行摘要；返回按钮改用统一规格

## 4. 逐页替换：对外展示页视觉归统

- [x] 4.1 统计页：改用管理台底色、卡片与控件规格；Tab 与聚合切换器共用同一分段控件；输入框与查询按钮统一高度
  - 补充（后续复核）：底色与 Tab/聚合分段器此前已落地，但 ApiKeyInput 整张输入卡未动——
    rounded-3xl 配 50px 扩散阴影与 hover 位移、输入框 2px 边框 / 12px 圆角 / 16px 字号、
    查询按钮本地重定义 .btn/.btn-primary/.btn-query（实测 52px 高）。已全部收到管理台基线：
    卡片 16px 圆角 + 轻阴影，输入框与按钮 32px / r8px，按钮改用全局 .btn-md。
    顺带删除模板零引用的 .gradient-bg / .gradient-bg-dark 及其 ::before 装饰层。
- [x] 4.2 统计页：在查询前补一排公共概览指标，避免首屏无内容
- [x] 4.3 落地页、教程页、引导页：控件与卡片替换为统一基线；品牌色收窄为主按钮与高亮色
  - 复核结论：符合，无需改动。落地页 .btn 为 980px 胶囊 + 17/19px 字号，是营销页刻意的
    设计语言（文件内已有注释声明 .btn--primary 为品牌色不改），且品牌色确实只用在主按钮与
    高亮色上，与「管理台控件 32px 基线」不冲突——后者约束的是管理页工具条控件。
- [ ] 4.4 登录页与用户登录页：两套登录表单收敛为一套实现，控件统一规格
  - 控件规格已统一（两输入框改 form-input-md、提交按钮改 btn-md）；两套表单未合并：
    LoginView 走 authStore.login（管理员认证）、UserLoginView 走 userStore.login（用户体系认证），
    属两套独立认证系统，且本部署 ldapEnabled=false 导致用户登录流程无法实测，
    合并却只能验证其中一条登录路径，风险不可接受。
- [x] 4.5 移除登录页不产生任何效果的「记住我」勾选框

## 5. 能力暴露与主题覆盖

- [x] 5.1 将用户管理页路由改为按用户体系开关条件注册，与导航注入引用同一状态
- [ ] 5.2 将 API Keys 已删除子页按同一规则处理可达性
  - 前提核查不成立，暂缓：该页非无效页面。后端软删除（isDeleted）真实生效，
    并配有 /api-keys/:keyId/restore 与 deleted/clear-all 接口，前端有完整恢复交互；
    当前部署恰好无已删除 Key 才显示为空。改为不可达会移除误删恢复的唯一入口，
    需先确认是否仍要执行。
- [ ] 5.3 移除依赖未启用特性的所有者与创建者列，以及在当前数据形态下永不成立的分支
  - 前半前提核查不成立，暂缓：创建者列与所有者提示已由 v-if="isLdapEnabled" 正确条件渲染，
    用户体系未启用时本就不渲染。直接移除会砍掉启用该体系的部署的功能，属能力回退。
    后半「永不成立的分支」未发现与配置无关的恒假分支，故未改动。
- [x] 5.4 修正停留在无导航项页面时导航误标高亮的问题
- [x] 5.5 将顶栏两个主题控件合并为单一入口下拉，面板内同时提供配色方案与明暗模式，并移除其中纯装饰的内部结构
- [x] 5.6 移除主题组件中无调用点的两个模式分支
- [x] 5.7 为登录页补齐暗色变体
- [x] 5.8 为 `components/user/` 下各组件补齐暗色变体，消除白卡压深底
- [x] 5.9 将教程页嵌套演示区的硬编码底色改为随主题变化
- [x] 5.10 移除各页面重复调用的主题初始化，仅保留应用根层一处

## 6. 组件收敛与死代码移除

- [ ] 6.1 抽取共用使用记录表格组件，将实体标签、取数方式、筛选维度与可选列作为参数传入
  - 暂缓：两页结构约 86% 相同，但差异为实质性——跨实体筛选方向相反、账户页多 platform
    参数与渠道展示、调用不同接口。已先完成两页可独立验证的改造（3.11），组件化留待单独一轮。
- [ ] 6.2 将两个使用记录页改为复用该组件，验证筛选、排序、分页、导出行为一致
  - 依赖 6.1，同步暂缓。
- [x] 6.3 抽取对外页共用导航与页脚布局，替换各页重复实现并收敛重复的 scoped 导航样式
- [x] 6.4 统计卡收敛为单一实现，各页面改为复用
- [x] 6.5 移除 `views/BalanceScriptsView.vue` 及 `utils/http_apis.js` 中随之失效的三个接口函数
- [x] 6.6 移除零引用的 `components/common/StatCard.vue` 与 `components/layout/TabBar.vue`
- [x] 6.7 移除仅存于注释的 `components/apikeys/LimitBadge.vue` 及 `ApiKeysView` 中被注释掉的死函数
- [x] 6.8 移除遗留的调试输出语句与声明为待接入的静态图表占位
- [x] 6.9 移除教程页无引用的死样式类与两套重复的教程调度逻辑之一

## 7. 构建与回归验证

- [x] 7.1 重新构建 SPA 并发布产物（构建前清理依赖目录再安装，避免锁文件冲突）
- [x] 7.2 回归：逐页检查同一行内并排控件高度一致且对齐同一基线
- [x] 7.3 回归：在常规宽度视口下检查各列表页表格无横向滚动
- [x] 7.4 回归：检查数值列右对齐、小数点成列、点击表头可排序
- [x] 7.5 回归：在无余额语义账户的部署下确认看板不渲染余额区块
- [x] 7.6 回归：用户体系未启用时直接输入用户管理页地址，确认落到未找到状态
- [x] 7.7 回归：暗色模式下逐页检查无白卡压深底、无混色区域
- [x] 7.8 回归：主题入口可同时切换配色方案与明暗模式，且与相邻控件对齐
- [ ] 7.9 回归：两个使用记录页由同一组件渲染且行为一致
  - 依赖 6.1/6.2，无法验证「由同一组件渲染」；已改为逐页核对：两页汇总摘要、数值列右对齐
    与等宽数字、返回按钮规格三项表现一致。
- [x] 7.10 回归：构建产物中不含已移除组件的引用，各页面功能不受影响
